import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const[engine,route,migrations,options,panel,guidance]=await Promise.all(["../lib/ai-investment-decision-engine.ts","../app/api/ai/decision/route.ts","../db/migrations.ts","../app/api/market/options/route.ts","../app/ai-analysis-evidence.tsx","../app/api/action-guidance/route.ts"].map(x=>readFile(new URL(x,import.meta.url),"utf8")));assert.match(engine,/INSUFFICIENT_CONFIRMATION/);assert.match(engine,/assessDecisionData/);assert.match(engine,/DecisionModelAdapter/);assert.match(engine,/calibrationFactor/);assert.match(engine,/ai_decision_runs/);assert.doesNotMatch(engine,/api\.openai\.com/);assert.match(route,/Investment account not found/);assert.match(migrations,/0025_ai_decision_engine_options/);assert.match(migrations,/CHAMPION.*CHALLENGER/);assert.match(migrations,/WALK_FORWARD/);assert.match(migrations,/ai_decision_outcomes/);assert.match(options,/contractMultiplier:100/);assert.match(options,/openInterest/);assert.match(options,/theta/);assert.match(options,/NO OPTION TRADE/);assert.match(panel,/Contradicting Evidence/);assert.match(panel,/What Would Change/);assert.match(guidance,/decideInvestment/);console.log("Central decision contract, privacy gate, immutable audit schema, evidence UI, and account-scoped Calls & Puts checks passed");
