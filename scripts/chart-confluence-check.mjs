import assert from "node:assert/strict";
import {readFile} from "node:fs/promises";
const chart=await readFile(new URL("../app/integrated-research-chart.tsx",import.meta.url),"utf8"),css=await readFile(new URL("../app/integrated-research-chart.css",import.meta.url),"utf8");
assert.match(chart,/timeframe==="5m"\|\|timeframe==="1h"/);
assert.match(chart,/series\(closes,50\)/);
assert.match(chart,/fib618/);
assert.match(chart,/level===\.618/);
assert.match(chart,/volumeRatio>=1\.2/);
assert.match(chart,/BUY ONLY ABOVE/);
assert.match(chart,/invalidation/);
assert.match(chart,/onPointerMove/);
assert.match(chart,/setPointerCapture/);
assert.match(chart,/O \$\{selected\.open/);
assert.match(chart,/YOUR PRICE PLAN · ALL TIMEFRAMES/);
assert.match(chart,/maximumAccountRiskPercent/);
assert.match(chart,/reward\/risk is at least 2:1/);
assert.match(chart,/Save and monitor this price plan/);
assert.match(chart,/\/api\/planned-actions/);
assert.match(css,/\.fib-ma-alert/);
assert.match(css,/@media\(max-width:390px\)/);
console.log("5m/1h Fibonacci 0.618, MA50, candle, volume, trigger, invalidation, pointer interaction, manual price plans, conservative sizing, durable monitoring, and mobile alert layout verified.");