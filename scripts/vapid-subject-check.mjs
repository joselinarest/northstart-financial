import assert from 'node:assert/strict';import {vapidSubject} from '../lib/vapid-subject.ts';
assert.equal(vapidSubject({APP_URL:'https://northstar.example.com'}),'https://northstar.example.com');assert.equal(vapidSubject({EMAIL_FROM:'sender@example.com'}),'mailto:sender@example.com');assert.equal(vapidSubject({EMAIL_FROM:'Northstar <sender@example.com>'}),'mailto:sender@example.com');assert.equal(vapidSubject({EMAIL_FROM:'security@northstar.local',APP_URL:'https://northstar.example.com'}),'https://northstar.example.com');assert.throws(()=>vapidSubject({APP_URL:'https://localhost'}),/PUBLIC_VAPID/);console.log('PASS: Apple-compatible VAPID subject, plain/display-name email and no local placeholders.');
