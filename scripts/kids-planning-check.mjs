import fs from "node:fs";
const read=p=>fs.readFileSync(p,'utf8'),migration=read('db/migrations.ts'),engine=read('lib/kids-planning.ts'),api=read('app/api/kids-planning/route.ts'),ui=read('app/kids-goals-center.tsx'),worker=read('app/api/notifications/process/route.ts'),workspace=read('app/northstar-workspace.tsx');
const required=[['child_profiles','profiles'],['child_goals','independent goals'],['child_goal_accounts','account links'],['child_plan_projections','projection history'],['child_contributions','contribution tracking'],['child_plan_reviews','annual reviews'],['child_plan_recommendations','recommendations']];
const checks=[...required.map(([token,name])=>[migration.includes(token),name]),[engine.includes('requiredMonthly')&&engine.includes('futureValue')&&engine.includes('collegeCost'),'deterministic calculations'],[engine.includes("goal==='FUTURE_WEALTH'")&&engine.includes("goal_type==='EDUCATION'"),'separate education and wealth allocation'],[engine.includes('affordable')||api.includes('affordableContributionHighCents'),'household affordability'],[engine.includes('accountTypeGuidance')&&engine.includes('irs.gov')&&engine.includes('studentaid.gov'),'authoritative account guidance'],[worker.includes("'KIDS_PLAN_REVIEW'")&&worker.includes('reviewKidsPlans'),'closed-app worker review'],[api.includes("action==='CREATE_CHILD'")&&api.includes("action==='CREATE_GOAL'")&&api.includes("action==='LINK_ACCOUNT'"),'profile goal account workflows'],[ui.includes('ON TRACK')||ui.includes('funding_status'),'visible funding status'],[engine.includes('childPortfolioModel')&&engine.includes("ticker:'VTI'")&&engine.includes("ticker:'QQQM'")&&engine.includes("ticker:'SCHD'"),'goal-aware ETF and dividend portfolio model'],[api.includes('childPortfolioModel')&&ui.includes('SUGGESTED ETF PORTFOLIO'),'portfolio model API and layout'],[workspace.includes('Kids / Goals')&&workspace.includes('<KidsGoalsCenter'),'navigation and module render'],[ui.includes('@media')||read('app/globals.css').includes('.kids-goals-center'),'mobile styling']];
const failed=checks.filter(([ok])=>!ok);if(failed.length){console.error(failed.map(([,n])=>`Missing: ${n}`).join('\n'));process.exit(1)}console.log('Kids profiles, independent goals, account links, deterministic projections, affordability, reviews, alerts, and mobile dashboard verified.');