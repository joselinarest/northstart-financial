import {build} from 'esbuild';import assert from 'node:assert/strict';
await build({entryPoints:['lib/server-configuration.ts'],outfile:'work/config-test.js',bundle:true,platform:'node',format:'esm'});const {inspectServerConfiguration,validateRequiredServerConfiguration}=await import('../work/config-test.js');
assert.throws(()=>validateRequiredServerConfiguration({DATABASE_URL:'credential-secret-invalid'}),/INVALID_POSTGRES_URL/);try{validateRequiredServerConfiguration({DATABASE_URL:'credential-secret-invalid'})}catch(e){assert(!e.message.includes('credential-secret-invalid'))}
assert.equal(inspectServerConfiguration({DATABASE_URL:'postgres://u:p@db/app',QUANT_DATA_API_KEY:undefined}).required.length,0);assert.equal(inspectServerConfiguration({DATABASE_URL:'postgres://u:p@db/app',ALPACA_API_KEY:'secret'}).optional[0].code,'INCOMPLETE_PROVIDER_CONFIGURATION');assert.equal(inspectServerConfiguration({DATABASE_URL:'postgres://u:p@db/app',COGNITO_CLIENT_ID:'a',NEXT_PUBLIC_COGNITO_CLIENT_ID:'b'}).required[0].code,'CLIENT_ALIAS_MISMATCH');assert.throws(()=>validateRequiredServerConfiguration({DATABASE_URL:'postgres://u:p@db/app',NODE_ENV:'production',APP_URL:'http://localhost:3100'}),/INVALID_APP_ORIGIN/);console.log('PASS configuration validation, optional-provider isolation, alias mismatch and secret-safe errors');
