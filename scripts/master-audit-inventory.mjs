import fs from 'node:fs';import path from 'node:path';
const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);
const files=[...walk('app'),...walk('lib'),...walk('workers')].filter(p=>/\.(ts|tsx)$/.test(p));
const routes=files.filter(p=>/route\.ts$/.test(p)).map(p=>({file:p.replaceAll('\\','/'),route:'/'+p.replaceAll('\\','/').replace(/^app\//,'').replace(/\/route.ts$/,''),methods:[...fs.readFileSync(p,'utf8').matchAll(/export (?:async )?function (GET|POST|PUT|PATCH|DELETE)/g)].map(m=>m[1]),verification:'UNVERIFIED'}));
const requests=files.flatMap(p=>[...fs.readFileSync(p,'utf8').matchAll(/fetch\(\s*(["'`])([^"'`]+)\1/g)].map(m=>({file:p.replaceAll('\\','/'),url:m[2],verification:'STATIC_INVENTORY_ONLY'})));
const env=new Map();for(const p of files)for(const m of fs.readFileSync(p,'utf8').matchAll(/process\.env\.([A-Z][A-Z0-9_]+)/g)){const name=m[1];env.set(name,{name,classification:name.startsWith('NEXT_PUBLIC_')?'PUBLIC_BROWSER_SAFE':/KEY|SECRET|TOKEN|PASSWORD|DATABASE_URL/.test(name)?'SERVER_ONLY_SECRET':/ALPACA|PLAID|FINNHUB|OPENAI|QUANT|VAPID|RESEND|FRED|NEWS/.test(name)?'PROVIDER_CONFIG':'RUNTIME_DEPLOYMENT',files:[...(env.get(name)?.files||[]),p.replaceAll('\\','/')]})}
fs.mkdirSync('docs/audit',{recursive:true});fs.writeFileSync('docs/audit/api-environment-inventory.json',JSON.stringify({generatedAt:new Date().toISOString(),note:'Presence is not proof of correct request contract, authorization or runtime behavior.',routes,requests,environment:[...env.values()]},null,2));
const source=fs.readFileSync(process.argv[2]||'docs/audit/master-requirements.md','utf8');fs.writeFileSync('docs/audit/master-requirements.md',source);
const headings=[...source.matchAll(/^([A-Z][A-Z /+&-]+):/gm)].map(m=>m[1]);const rows=headings.filter(h=>!['FINAL DELIVERY','NORTHSTAR PRODUCT PURPOSE'].includes(h)).map(h=>`| ${h} | ${h==='CREDIT + DEBT INTELLIGENCE'?'BLOCKED BY EXTERNAL PROVIDER':'PARTIAL'} | Pending full trace; see API inventory | Not determined | End-to-end trace pending | Not fully executed | No completion claim | ${h==='CREDIT + DEBT INTELLIGENCE'?'Approved credit provider and hosted identity/consent contract required':'Requirement is not accepted until runtime evidence is recorded'} |`);
if(!fs.existsSync('docs/audit/master-verification.md'))fs.writeFileSync('docs/audit/master-verification.md',`# Northstar master verification — in progress\n\nBaseline deployed commit: 50ad490. This audit does not certify the full product. Existing source-only checks are distinguished from behavioral database/browser tests. Real trades are never executed.\n\n## Repaired and tested during this pass\n\n| FEATURE | STATUS | FILES CHANGED | DB MIGRATION | API/WORKER/PROVIDER | TEST EXECUTED | ACTUAL RESULT | REMAINING LIMITATION |\n|---|---|---|---|---|---|---|---|\n| Manual holding persistence | PARTIAL | lib/manual-holdings.ts; app/api/connections/plaid/route.ts; account profile; workspace | None | Authenticated manual holdings POST | manual-holdings-check.mjs | PASS: 3 holdings, duplicate rejection, weighted merge, edit/date, delete, totals, isolation, database reread | Authenticated browser reload and live quotes still pending |\n| Exchange sessions | PARTIAL | lib/exchange-calendar.ts; market-session.ts; notification-worker.ts | None | Worker scheduling, market provider | exchange-calendar-check.mjs | PASS: published holidays, early closes, DST, expired calendar fail-closed | Live clock must handle exceptional closures; published calendar covers 2026–2028 |\n| Quote price integrity | PARTIAL | lib/providers/alpaca-market-data.ts; market-data.ts | None | Shared provider | exchange-calendar-check.mjs | PASS: last close and matching price timestamp | All frontend labels and historical-close freshness still need verification |\n| Central lifecycle | PARTIAL | integration test loader/assertions | None | PostgreSQL + injected providers | trade-lifecycle-integration-check.mjs | PASS: sale, rebuy, rotation, dedup, isolation, AI schema and outage handling | No live brokerage/push acceptance; strategy-position integration incomplete |\n| Portfolio snapshots | PARTIAL | lib/continuous-intelligence-loop.ts | None | Account worker | Type check pending final run | Mark fetched quotes; preserve partial state for missing marks; manual accounts no longer require Plaid timestamp | Dedicated database behavior test pending |\n\n## Complete requirement ledger\n\nThe entries below remain conservative until their entire acceptance path is verified.\n\n| FEATURE | STATUS | FILES CHANGED | DB MIGRATION | API/WORKER/PROVIDER | TEST EXECUTED | ACTUAL RESULT | REMAINING LIMITATION |\n|---|---|---|---|---|---|---|---|\n${rows.join('\n')}\n\n## Evidence\n\n- API/request/environment inventory: api-environment-inventory.json (static inventory only).\n- Baseline test run: baseline-tests.json. PASS means the named test passed, not that the entire feature is complete.\n- Exchange calendar source: https://www.nyse.com/markets/hours-calendars\n- Quant Data production key was absent at the previous release verification; do not fabricate flow.\n- Production authenticated full-route UI and push-device receipt tests are still pending.\n`);
fs.copyFileSync('work/master-audit-tests.json','docs/audit/baseline-tests.json');console.log(routes.length+' API routes, '+requests.length+' literal request sites, '+env.size+' environment variables, '+headings.length+' requirement groups inventoried');
