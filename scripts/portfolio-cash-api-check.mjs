import assert from 'node:assert/strict';
import {registerHooks} from 'node:module';
import {PGlite} from '@electric-sql/pglite';
registerHooks({resolve(s,c,next){if(s==='@/lib/db')return{url:'data:text/javascript,export const workspace=async()=>globalThis.testWorkspace',shortCircuit:true};if(s.startsWith('@/lib/'))return{url:new URL('../lib/'+s.slice(6)+'.ts',import.meta.url).href,shortCircuit:true};if(s==='./cash-allocation')return{url:new URL('../lib/domain/cash-allocation.ts',import.meta.url).href,shortCircuit:true};return next(s,c)}});
const pg=new PGlite();await pg.exec(`CREATE TABLE entities(id text,household_id text);CREATE TABLE accounts(id text,entity_id text,nickname text,name text,investment_purpose text,type text,subtype text,current_balance_cents bigint);CREATE TABLE investment_account_settings(account_id text,strategy_type text,share_mode text,maximum_position_bps int,available_cash_cents bigint,horizon_months int,policy_json jsonb);CREATE TABLE holdings(id text,security_id text,account_id text,quantity numeric,price_cents bigint,cost_basis_cents bigint);CREATE TABLE securities(id text,ticker text,name text,type text);CREATE TABLE account_allocation_targets(account_id text,category text,target_bps int);CREATE TABLE liabilities(entity_id text,current_balance_cents bigint,apr_bps int);CREATE TABLE transactions(entity_id text,amount_cents bigint,direction text,excluded int,posted_at timestamptz);CREATE TABLE trade_lifecycle_exits(account_id text,status text,allocation_json jsonb,plan_json jsonb);INSERT INTO entities VALUES('e','h');INSERT INTO accounts VALUES('a','e',null,'Test',null,'investment',null,0);INSERT INTO investment_account_settings VALUES('a','CUSTOM','WHOLE',6000,100000,60,'{}');INSERT INTO securities VALUES('s1','AAA','Index fund','etf'),('s2','BBB','Index fund','etf');INSERT INTO holdings VALUES('h1','s1','a',10,10000,90000),('h2','s2','a',10,10000,90000);INSERT INTO account_allocation_targets VALUES('a','CORE',9000),('a','QUALITY',0),('a','AGGRESSIVE',0),('a','INCOME',0),('a','BONDS',0),('a','CASH',1000);INSERT INTO trade_lifecycle_exits VALUES('a','REENTRY_WATCH',null,'{"reservedCash":200}');`);
const db={prepare(sql){let values=[];return{bind(...v){values=v;return this},async all(){let n=0;return{results:(await pg.query(sql.replace(/\?/g,()=>`$${++n}`),values)).rows}},async first(){return(await this.all()).results[0]||null}}}};
globalThis.testWorkspace={db,householdId:'h'};const {GET}=await import('../app/api/portfolio/advice/route.ts');
let response=await GET(new Request('http://test/api/portfolio/advice?accountId=a'));let body=await response.json();assert.equal(response.status,200,JSON.stringify(body));assert.equal(body.portfolio.cashAllocation.reservedReentryCents,'20000');assert.equal(body.portfolio.cashAllocation.deployableCents,'50000');assert.equal(body.portfolio.cashAllocation.estimatedCostCents,'40000');assert.equal(body.portfolio.holdings.reduce((s,r)=>s+Number(r.amountCents),0),40000);
globalThis.testWorkspace.householdId='other';assert.equal((await GET(new Request('http://test/api/portfolio/advice?accountId=a'))).status,404);await pg.close();console.log('PASS: real portfolio endpoint SQL, saved targets and cash, reentry reserves, shared holding budget, household isolation');
