import React,{useEffect,useState} from 'react';import {createRoot} from 'react-dom/client';import Profile from '../app/investment-account-profile';
function App(){const[data,setData]=useState<any>(null),[error,setError]=useState('');const reload=async()=>setData(await fetch('/fixture').then(r=>r.json()));useEffect(()=>{reload()},[]);if(!data)return <p>Loading</p>;return <main className="northstar-workspace"><p role="status">{error}</p><Profile account={data.account} holdings={data.holdings} onSave={async()=>true} onAddHolding={async(accountId,holding)=>{const r=await fetch('/fixture',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({accountId,...holding})});const v=await r.json();if(!r.ok){setError(v.error);return false}setError('');await reload();return true}}/></main>}createRoot(document.getElementById('root')!).render(<App/>);
