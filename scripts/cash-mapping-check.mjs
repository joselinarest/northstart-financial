import {build} from 'esbuild';import {writeFile} from 'node:fs/promises';
await writeFile('work/cash-mapping-test.ts',`import assert from 'node:assert/strict';import {reconcileCash,cashSymbols,investmentCash} from '../lib/investment-cash';import {PGlite} from '@electric-sql/pglite';
async function main(){assert.equal(reconcileCash(84,4175,[{symbol:"",securityType:"cash",valueCents:4175}],[]).mappedCents,4175);assert.deepEqual(cashSymbols({}),['QDERQ','SPAXX']);assert.deepEqual(cashSymbols({cashSymbols:[]}),[]);assert.equal(reconcileCash(84,4175,[{symbol:'QDERQ',valueCents:4175}],cashSymbols({})).cashCents,4175);assert.equal(reconcileCash(100000,100000,[{symbol:'SPAXX',valueCents:100000}],cashSymbols({})).cashCents,100000);assert.equal(reconcileCash(84,0,[{symbol:'SPAXX',valueCents:100000}],[]).cashCents,84);
const pg=new PGlite();await pg.exec("CREATE TABLE entities(id text,household_id text);CREATE TABLE accounts(id text,entity_id text,connection_id text,available_balance_cents bigint,type text);CREATE TABLE investment_account_settings(account_id text,available_cash_cents bigint,policy_json jsonb);CREATE TABLE securities(id text,ticker text,currency text,type text);CREATE TABLE holdings(account_id text,security_id text,quantity numeric,price_cents bigint);INSERT INTO entities VALUES('e','h');INSERT INTO accounts VALUES('a','e','connected',4175,'investment'),('b','e','connected',100000,'investment');INSERT INTO investment_account_settings VALUES('a',84,'{}'),('b',0,'{}');INSERT INTO securities VALUES('q','QDERQ','USD','other'),('s','SPAXX','USD','mutual fund'),('o','ORCL','USD','equity');INSERT INTO holdings VALUES('a','q',41.75,100),('b','s',1000,100),('a','o',2,15000);");const db={prepare(sql:string){let n=0;sql=sql.replace(/\\?/g,()=>'$'+(++n));return {bind(...args:any[]){return {first:async()=> (await pg.query(sql,args)).rows[0],all:async()=>({results:(await pg.query(sql,args)).rows})}}}}} as any;
const a=await investmentCash(db,'h','a'),b=await investmentCash(db,'h','b');assert.equal(a.cashCents,4175);assert.equal(a.mappedCents,4175);assert.equal(a.largestInvestedCents,30000);assert.equal(b.cashCents,100000);await assert.rejects(investmentCash(db,'other','a'));await pg.exec("UPDATE investment_account_settings SET available_cash_cents=999999 WHERE account_id='a'");assert.equal((await investmentCash(db,'h','a')).cashCents,4175);await pg.exec(\"UPDATE investment_account_settings SET policy_json='{\\\"cashSymbols\\\":[]}' WHERE account_id='a'\");assert.equal((await investmentCash(db,'h','a')).mappedCents,0);await pg.close();console.log('PASS cash reconciliation, no double counting, account isolation, configurable mapping and invested exposure.');}main().catch(e=>{console.error(e);process.exit(1)});`);
await build({entryPoints:['work/cash-mapping-test.ts'],outfile:'work/cash-mapping-test.cjs',bundle:true,platform:'node',packages:'external',logLevel:'silent'});await import('../work/cash-mapping-test.cjs');

