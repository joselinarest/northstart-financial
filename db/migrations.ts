export type Migration = {
  id: string;
  description: string;
  statements: readonly string[];
};

export const migrations: readonly Migration[] = [
  {
    id: "0009_investment_domain_foundation",
    description: "Account strategies, investment ledger, theses, recommendations, and predictions",
    statements: [
      `CREATE TABLE IF NOT EXISTS investment_account_settings (
        account_id TEXT PRIMARY KEY REFERENCES accounts(id) ON DELETE CASCADE,
        strategy_type TEXT NOT NULL CHECK(strategy_type IN ('SWING','GROWTH_5_7','RETIREMENT','CHILD_GROWTH','COLLEGE','AGGRESSIVE_GROWTH','DIVIDEND_INCOME','HOUSE_FUND','CAPITAL_PRESERVATION','CUSTOM')),
        share_mode TEXT NOT NULL DEFAULT 'WHOLE' CHECK(share_mode IN ('WHOLE','FRACTIONAL')),
        benchmark_symbol TEXT,
        goal_name TEXT NOT NULL,
        horizon_months INTEGER,
        risk_profile TEXT NOT NULL DEFAULT 'BALANCED' CHECK(risk_profile IN ('CONSERVATIVE','BALANCED','GROWTH','AGGRESSIVE')),
        maximum_position_bps INTEGER NOT NULL DEFAULT 1000 CHECK(maximum_position_bps BETWEEN 0 AND 10000),
        maximum_risk_bps INTEGER NOT NULL DEFAULT 50 CHECK(maximum_risk_bps BETWEEN 0 AND 10000),
        available_cash_cents BIGINT NOT NULL DEFAULT 0,
        policy_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS account_allocation_targets (
        id TEXT PRIMARY KEY,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        category TEXT NOT NULL,
        target_bps INTEGER NOT NULL CHECK(target_bps BETWEEN 0 AND 10000),
        minimum_bps INTEGER CHECK(minimum_bps BETWEEN 0 AND 10000),
        maximum_bps INTEGER CHECK(maximum_bps BETWEEN 0 AND 10000),
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(account_id, category)
      )`,
      `CREATE TABLE IF NOT EXISTS investment_transactions (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id),
        account_id TEXT NOT NULL REFERENCES accounts(id),
        security_id TEXT REFERENCES securities(id),
        transaction_type TEXT NOT NULL CHECK(transaction_type IN ('BUY','SELL','DIVIDEND','DEPOSIT','WITHDRAWAL','INTEREST','FEE','TRANSFER','SPLIT')),
        trade_at TIMESTAMPTZ NOT NULL,
        settle_at TIMESTAMPTZ,
        quantity NUMERIC(28,8),
        price_cents BIGINT,
        amount_cents BIGINT NOT NULL,
        fee_cents BIGINT NOT NULL DEFAULT 0,
        currency TEXT NOT NULL DEFAULT 'USD',
        source TEXT NOT NULL CHECK(source IN ('MANUAL','PLAID','IMPORT')),
        external_id TEXT,
        transfer_account_id TEXT REFERENCES accounts(id),
        split_numerator NUMERIC(28,8),
        split_denominator NUMERIC(28,8),
        notes TEXT,
        reverses_transaction_id TEXT REFERENCES investment_transactions(id),
        created_by_user_id TEXT NOT NULL REFERENCES users(id),
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(account_id, source, external_id)
      )`,
      `CREATE TABLE IF NOT EXISTS tax_lots (
        id TEXT PRIMARY KEY,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        security_id TEXT NOT NULL REFERENCES securities(id),
        opening_transaction_id TEXT NOT NULL REFERENCES investment_transactions(id),
        acquired_at TIMESTAMPTZ NOT NULL,
        original_quantity NUMERIC(28,8) NOT NULL CHECK(original_quantity > 0),
        remaining_quantity NUMERIC(28,8) NOT NULL CHECK(remaining_quantity >= 0),
        total_basis_cents BIGINT NOT NULL,
        remaining_basis_cents BIGINT NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS tax_lot_disposals (
        id TEXT PRIMARY KEY,
        lot_id TEXT NOT NULL REFERENCES tax_lots(id),
        sell_transaction_id TEXT NOT NULL REFERENCES investment_transactions(id),
        quantity NUMERIC(28,8) NOT NULL CHECK(quantity > 0),
        basis_cents BIGINT NOT NULL,
        proceeds_cents BIGINT NOT NULL,
        realized_pnl_cents BIGINT NOT NULL,
        disposed_at TIMESTAMPTZ NOT NULL,
        UNIQUE(lot_id, sell_transaction_id)
      )`,
      `CREATE TABLE IF NOT EXISTS investment_theses (
        id TEXT PRIMARY KEY,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        security_id TEXT NOT NULL REFERENCES securities(id),
        state TEXT NOT NULL CHECK(state IN ('STRONG','INTACT','WATCH','WEAKENING','BROKEN')),
        thesis TEXT NOT NULL,
        invalidation TEXT NOT NULL,
        evidence_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        reviewed_at TIMESTAMPTZ NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(account_id, security_id)
      )`,
      `CREATE TABLE IF NOT EXISTS recommendations (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id),
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        security_id TEXT NOT NULL REFERENCES securities(id),
        strategy_type TEXT NOT NULL,
        action TEXT NOT NULL CHECK(action IN ('STRONG_BUY','BUY','BUY_PARTIAL','ACCUMULATE','HOLD','WAIT','WATCH','TAKE_PARTIAL_PROFIT','REDUCE','SELL','EXIT','THESIS_REVIEW','THESIS_BROKEN','EVENT_RISK')),
        lifecycle TEXT NOT NULL CHECK(lifecycle IN ('MONITORING','TRIGGERED','EXPIRED','CANCELLED','INVALIDATED','COMPLETED','MISSED')),
        suggested_quantity NUMERIC(28,8) NOT NULL DEFAULT 0,
        entry_low_cents BIGINT,
        entry_high_cents BIGINT,
        ideal_price_cents BIGINT,
        invalidation_cents BIGINT,
        targets_json JSONB NOT NULL DEFAULT '[]'::jsonb,
        confidence INTEGER NOT NULL CHECK(confidence BETWEEN 0 AND 100),
        reason TEXT NOT NULL,
        checks_json JSONB NOT NULL,
        actionable BOOLEAN NOT NULL DEFAULT FALSE,
        model_version TEXT NOT NULL,
        evidence_as_of TIMESTAMPTZ NOT NULL,
        expires_at TIMESTAMPTZ NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS recommendation_triggers (
        id TEXT PRIMARY KEY,
        recommendation_id TEXT NOT NULL REFERENCES recommendations(id) ON DELETE CASCADE,
        trigger_type TEXT NOT NULL,
        condition_json JSONB NOT NULL,
        cooldown_until TIMESTAMPTZ,
        triggered_at TIMESTAMPTZ,
        last_evaluated_at TIMESTAMPTZ,
        status TEXT NOT NULL DEFAULT 'MONITORING'
      )`,
      `CREATE TABLE IF NOT EXISTS predictions (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id),
        account_id TEXT REFERENCES accounts(id) ON DELETE CASCADE,
        security_id TEXT NOT NULL REFERENCES securities(id),
        horizon TEXT NOT NULL,
        model_version TEXT NOT NULL,
        feature_fingerprint TEXT NOT NULL,
        input_snapshot_json JSONB NOT NULL,
        scenarios_json JSONB NOT NULL,
        confidence INTEGER NOT NULL CHECK(confidence BETWEEN 0 AND 100),
        evidence_as_of TIMESTAMPTZ NOT NULL,
        expires_at TIMESTAMPTZ NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS prediction_points (
        id TEXT PRIMARY KEY,
        prediction_id TEXT NOT NULL REFERENCES predictions(id) ON DELETE CASCADE,
        scenario TEXT NOT NULL CHECK(scenario IN ('BULL','BASE','BEAR')),
        point_at TIMESTAMPTZ NOT NULL,
        price_cents BIGINT NOT NULL,
        lower_cents BIGINT,
        upper_cents BIGINT,
        UNIQUE(prediction_id, scenario, point_at)
      )`,
      `CREATE TABLE IF NOT EXISTS prediction_evaluations (
        prediction_id TEXT PRIMARY KEY REFERENCES predictions(id) ON DELETE CASCADE,
        actual_price_cents BIGINT NOT NULL,
        actual_at TIMESTAMPTZ NOT NULL,
        direction_correct BOOLEAN,
        inside_expected_range BOOLEAN,
        absolute_error_bps INTEGER,
        evaluated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS portfolio_snapshots (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id),
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        captured_at TIMESTAMPTZ NOT NULL,
        market_value_cents BIGINT NOT NULL,
        cash_cents BIGINT NOT NULL,
        cost_basis_cents BIGINT NOT NULL,
        realized_pnl_cents BIGINT NOT NULL,
        unrealized_pnl_cents BIGINT NOT NULL,
        allocation_json JSONB NOT NULL,
        source_freshness_json JSONB NOT NULL,
        UNIQUE(account_id, captured_at)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_investment_transactions_account_time ON investment_transactions(account_id, trade_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_investment_transactions_household_time ON investment_transactions(household_id, trade_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_tax_lots_account_security ON tax_lots(account_id, security_id, acquired_at)`,
      `CREATE INDEX IF NOT EXISTS idx_recommendations_account_created ON recommendations(account_id, created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_recommendations_symbol_lifecycle ON recommendations(security_id, lifecycle, expires_at)`,
      `CREATE INDEX IF NOT EXISTS idx_predictions_security_created ON predictions(security_id, created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_portfolio_snapshots_account_time ON portfolio_snapshots(account_id, captured_at DESC)`,
    ],
  },
  {
    id: "0010_persistent_alert_delivery",
    description: "Persistent alert delivery state and notification preferences",
    statements: [
      `CREATE TABLE IF NOT EXISTS alert_deliveries (
        id TEXT PRIMARY KEY,
        alert_id TEXT NOT NULL REFERENCES alerts(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        channel TEXT NOT NULL CHECK(channel IN ('IN_APP','BROWSER_PUSH','EMAIL')),
        status TEXT NOT NULL DEFAULT 'PENDING' CHECK(status IN ('PENDING','DELIVERED','FAILED','DISMISSED')),
        attempted_at TIMESTAMPTZ,
        delivered_at TIMESTAMPTZ,
        error_code TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(alert_id,user_id,channel)
      )`,
      `CREATE TABLE IF NOT EXISTS notification_preferences (
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        in_app_enabled BOOLEAN NOT NULL DEFAULT TRUE,
        browser_push_enabled BOOLEAN NOT NULL DEFAULT FALSE,
        email_enabled BOOLEAN NOT NULL DEFAULT FALSE,
        quiet_hours_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        timezone TEXT NOT NULL DEFAULT 'America/Phoenix',
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY(household_id,user_id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_alerts_household_unread ON alerts(household_id,read_at,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_alert_deliveries_user_status ON alert_deliveries(user_id,status,created_at DESC)`,
    ],
  },
  {
    id: "0011_planned_actions",
    description: "Persistent non-executing action guidance plans",
    statements: [
      `CREATE TABLE IF NOT EXISTS planned_actions (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id),
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        security_id TEXT REFERENCES securities(id),
        recommendation_id TEXT REFERENCES recommendations(id),
        action_type TEXT NOT NULL CHECK(action_type IN ('BUY_NOW','PLACE_ON_WATCH','WAIT_FOR_PRICE','BUY_ON_CONFIRMATION','SELL','REDUCE','TAKE_PROFIT','HOLD','DO_NOTHING')),
        lifecycle TEXT NOT NULL CHECK(lifecycle IN ('PROPOSED','MONITORING','READY','TRIGGERED','INVALIDATED','EXPIRED','COMPLETED','MISSED')),
        priority TEXT NOT NULL CHECK(priority IN ('CRITICAL','ACTION_NOW','READY_IF_PRICE_TRIGGERS','WATCH','NO_ACTION')),
        suggested_quantity NUMERIC(28,8) NOT NULL DEFAULT 0,
        target_total_quantity NUMERIC(28,8),
        entry_low_cents BIGINT,
        entry_high_cents BIGINT,
        preferred_price_cents BIGINT,
        invalidation_cents BIGINT,
        targets_json JSONB NOT NULL DEFAULT '[]'::jsonb,
        conditions_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        capital_source_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        rationale TEXT NOT NULL,
        evidence_snapshot_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        model_version TEXT NOT NULL,
        evidence_as_of TIMESTAMPTZ NOT NULL,
        expires_at TIMESTAMPTZ NOT NULL,
        executed_transaction_id TEXT REFERENCES investment_transactions(id),
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_planned_actions_account_priority ON planned_actions(account_id,lifecycle,priority,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_planned_actions_household_lifecycle ON planned_actions(household_id,lifecycle,expires_at)`,
      `CREATE INDEX IF NOT EXISTS idx_planned_actions_security ON planned_actions(security_id,account_id,created_at DESC)`,
    ],
  },
  {
    id: "0012_real_estate_operations",
    description: "Property ownership, operating activity, debt, occupancy, projects, watchlists, and historical performance",
    statements: [
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS use_type TEXT NOT NULL DEFAULT 'OTHER'`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS ownership_bps INTEGER NOT NULL DEFAULT 10000`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS acquisition_date DATE`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS include_in_net_worth BOOLEAN NOT NULL DEFAULT TRUE`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS include_in_cash_flow BOOLEAN NOT NULL DEFAULT TRUE`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'ACTIVE'`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS reserve_balance_cents BIGINT NOT NULL DEFAULT 0`,
      `ALTER TABLE properties ADD COLUMN IF NOT EXISTS target_reserve_cents BIGINT NOT NULL DEFAULT 0`,
      `CREATE TABLE IF NOT EXISTS property_loans (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        lender TEXT, loan_type TEXT NOT NULL DEFAULT 'FIXED', original_balance_cents BIGINT,
        current_balance_cents BIGINT NOT NULL, interest_rate_bps INTEGER, monthly_payment_cents BIGINT,
        principal_payment_cents BIGINT, interest_payment_cents BIGINT, next_due_at DATE,
        maturity_date DATE, rate_reset_at DATE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS property_transactions (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        transaction_id TEXT REFERENCES transactions(id) ON DELETE SET NULL, occurred_at DATE NOT NULL,
        direction TEXT NOT NULL CHECK(direction IN ('INCOME','EXPENSE')), category TEXT NOT NULL,
        amount_cents BIGINT NOT NULL, capital_improvement BOOLEAN NOT NULL DEFAULT FALSE,
        description TEXT, vendor TEXT, notes TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS property_occupancies (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        occupancy_type TEXT NOT NULL CHECK(occupancy_type IN ('TENANT','GUEST','OWNER','VACANT')),
        display_name TEXT, start_at DATE NOT NULL, end_at DATE, rent_cents BIGINT,
        deposit_cents BIGINT, status TEXT NOT NULL DEFAULT 'ACTIVE', metadata_json JSONB NOT NULL DEFAULT '{}'::jsonb
      )`,
      `CREATE TABLE IF NOT EXISTS property_projects (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        title TEXT NOT NULL, project_type TEXT NOT NULL DEFAULT 'MAINTENANCE', vendor TEXT,
        estimated_cost_cents BIGINT, actual_cost_cents BIGINT, status TEXT NOT NULL DEFAULT 'PLANNED',
        due_at DATE, warranty_expires_at DATE, notes TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS property_budgets (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        year INTEGER NOT NULL, category TEXT NOT NULL, budget_cents BIGINT NOT NULL,
        UNIQUE(property_id,year,category)
      )`,
      `CREATE TABLE IF NOT EXISTS property_snapshots (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        captured_at DATE NOT NULL, estimated_value_cents BIGINT NOT NULL, debt_cents BIGINT NOT NULL DEFAULT 0,
        income_cents BIGINT NOT NULL DEFAULT 0, expense_cents BIGINT NOT NULL DEFAULT 0,
        noi_cents BIGINT NOT NULL DEFAULT 0, occupancy_bps INTEGER, source TEXT NOT NULL,
        UNIQUE(property_id,captured_at)
      )`,
      `CREATE TABLE IF NOT EXISTS property_opportunities (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        name TEXT NOT NULL, address TEXT, strategy TEXT NOT NULL, asking_price_cents BIGINT,
        target_price_cents BIGINT, required_cap_rate_bps INTEGER, required_cash_flow_cents BIGINT,
        assumptions_json JSONB NOT NULL DEFAULT '{}'::jsonb, notes TEXT, status TEXT NOT NULL DEFAULT 'WATCH',
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_property_transactions_property_date ON property_transactions(property_id,occurred_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_property_loans_property ON property_loans(property_id)`,
      `CREATE INDEX IF NOT EXISTS idx_property_projects_property_status ON property_projects(property_id,status,due_at)`,
      `CREATE INDEX IF NOT EXISTS idx_property_snapshots_property_date ON property_snapshots(property_id,captured_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_property_opportunities_household_status ON property_opportunities(household_id,status,updated_at DESC)`,
    ],
  },
  {
    id: "0013_property_financial_account_links",
    description: "Central-account property roles, allocation-safe transaction context, and reusable classification rules",
    statements: [
      `CREATE TABLE IF NOT EXISTS property_account_links (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        role TEXT NOT NULL CHECK(role IN ('PRIMARY_INCOME','MORTGAGE_PAYMENT','OPERATING_EXPENSE','RESERVE','CREDIT_CARD','SECURITY_DEPOSIT','TAX_INSURANCE','LOAN','OTHER')),
        active BOOLEAN NOT NULL DEFAULT TRUE, notes TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(property_id,account_id,role)
      )`,
      `CREATE TABLE IF NOT EXISTS property_transaction_allocations (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        transaction_id TEXT NOT NULL REFERENCES transactions(id) ON DELETE CASCADE,
        allocated_amount_cents BIGINT NOT NULL, category TEXT NOT NULL,
        capital_improvement BOOLEAN NOT NULL DEFAULT FALSE, assignment_source TEXT NOT NULL DEFAULT 'MANUAL',
        reviewed BOOLEAN NOT NULL DEFAULT FALSE, notes TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(property_id,transaction_id,category)
      )`,
      `CREATE TABLE IF NOT EXISTS property_transaction_rules (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        property_id TEXT REFERENCES properties(id) ON DELETE CASCADE,
        account_id TEXT REFERENCES accounts(id) ON DELETE CASCADE,
        merchant_pattern TEXT, description_pattern TEXT, direction TEXT,
        property_category TEXT NOT NULL, assignment_mode TEXT NOT NULL DEFAULT 'PROMPT' CHECK(assignment_mode IN ('AUTOMATIC','PROMPT')),
        active BOOLEAN NOT NULL DEFAULT TRUE, priority INTEGER NOT NULL DEFAULT 100,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_property_account_links_account ON property_account_links(account_id,active,property_id)`,
      `CREATE INDEX IF NOT EXISTS idx_property_allocations_transaction ON property_transaction_allocations(transaction_id,property_id)`,
      `CREATE INDEX IF NOT EXISTS idx_property_rules_household_active ON property_transaction_rules(household_id,active,priority)`,
    ],
  },
  {
    id: "0014_transaction_notifications",
    description: "Idempotent transaction events, per-account preferences, push subscriptions, and delivery retries",
    statements: [
      `ALTER TABLE notification_preferences ADD COLUMN IF NOT EXISTS transaction_mode TEXT NOT NULL DEFAULT 'MATERIAL' CHECK(transaction_mode IN ('OFF','MATERIAL','EVERY_TRANSACTION'))`,
      `ALTER TABLE notification_preferences ADD COLUMN IF NOT EXISTS minimum_amount_cents BIGINT NOT NULL DEFAULT 0`,
      `ALTER TABLE notification_preferences ADD COLUMN IF NOT EXISTS transaction_filters_json JSONB NOT NULL DEFAULT '{}'::jsonb`,
      `ALTER TABLE notification_preferences ADD COLUMN IF NOT EXISTS suspicious_override BOOLEAN NOT NULL DEFAULT TRUE`,
      `ALTER TABLE notification_preferences ADD COLUMN IF NOT EXISTS fallback_json JSONB NOT NULL DEFAULT '{}'::jsonb`,
      `CREATE TABLE IF NOT EXISTS account_notification_preferences (
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        enabled BOOLEAN NOT NULL DEFAULT TRUE,
        transaction_mode TEXT NOT NULL DEFAULT 'INHERIT' CHECK(transaction_mode IN ('INHERIT','OFF','MATERIAL','EVERY_TRANSACTION')),
        channels_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        minimum_amount_cents BIGINT,
        filters_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY(household_id,user_id,account_id)
      )`,
      `CREATE TABLE IF NOT EXISTS transaction_notification_events (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        transaction_id TEXT REFERENCES transactions(id) ON DELETE SET NULL,
        event_type TEXT NOT NULL CHECK(event_type IN ('IMPORTED','UPDATED','PENDING_POSTED','RECURRING_IDENTIFIED','SUSPICIOUS','REMOVED')),
        idempotency_key TEXT NOT NULL UNIQUE,
        severity TEXT NOT NULL CHECK(severity IN ('INFO','WATCH','WARNING','CRITICAL')),
        snapshot_json JSONB NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS push_subscriptions (
        id TEXT PRIMARY KEY,
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        endpoint_hash TEXT NOT NULL UNIQUE,
        encrypted_subscription TEXT NOT NULL,
        active BOOLEAN NOT NULL DEFAULT TRUE,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS notification_delivery_attempts (
        id TEXT PRIMARY KEY,
        delivery_id TEXT NOT NULL REFERENCES alert_deliveries(id) ON DELETE CASCADE,
        attempt_number INTEGER NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('SENT','DELIVERED','FAILED')),
        provider_code TEXT,
        attempted_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        next_retry_at TIMESTAMPTZ,
        UNIQUE(delivery_id,attempt_number)
      )`,
      `ALTER TABLE alert_deliveries DROP CONSTRAINT IF EXISTS alert_deliveries_status_check`,
      `UPDATE alert_deliveries SET status='QUEUED' WHERE status='PENDING'`,
      `ALTER TABLE alert_deliveries ALTER COLUMN status SET DEFAULT 'QUEUED'`,
      `ALTER TABLE alert_deliveries ADD CONSTRAINT alert_deliveries_status_check CHECK(status IN ('PENDING','QUEUED','SENT','DELIVERED','FAILED','READ','DISMISSED'))`,
      `CREATE INDEX IF NOT EXISTS idx_transaction_notification_events_household_time ON transaction_notification_events(household_id,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_transaction_notification_events_account_time ON transaction_notification_events(account_id,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_notification_delivery_queue ON alert_deliveries(status,created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_notification_retry_due ON notification_delivery_attempts(next_retry_at) WHERE status='FAILED'`,
    ],
  },
  {
    id: "0015_repair_property_financial_context",
    description: "Create property financial-context tables for databases where the earlier migration was already recorded",
    statements: [
      `CREATE TABLE IF NOT EXISTS property_account_links (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        role TEXT NOT NULL CHECK(role IN ('PRIMARY_INCOME','MORTGAGE_PAYMENT','OPERATING_EXPENSE','RESERVE','CREDIT_CARD','SECURITY_DEPOSIT','TAX_INSURANCE','LOAN','OTHER')),
        active BOOLEAN NOT NULL DEFAULT TRUE, notes TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(property_id,account_id,role)
      )`,
      `CREATE TABLE IF NOT EXISTS property_transaction_allocations (
        id TEXT PRIMARY KEY, property_id TEXT NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
        transaction_id TEXT NOT NULL REFERENCES transactions(id) ON DELETE CASCADE,
        allocated_amount_cents BIGINT NOT NULL, category TEXT NOT NULL,
        capital_improvement BOOLEAN NOT NULL DEFAULT FALSE, assignment_source TEXT NOT NULL DEFAULT 'MANUAL',
        reviewed BOOLEAN NOT NULL DEFAULT FALSE, notes TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(property_id,transaction_id,category)
      )`,
      `CREATE TABLE IF NOT EXISTS property_transaction_rules (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        property_id TEXT REFERENCES properties(id) ON DELETE CASCADE,
        account_id TEXT REFERENCES accounts(id) ON DELETE CASCADE,
        merchant_pattern TEXT, description_pattern TEXT, direction TEXT,
        property_category TEXT NOT NULL, assignment_mode TEXT NOT NULL DEFAULT 'PROMPT' CHECK(assignment_mode IN ('AUTOMATIC','PROMPT')),
        active BOOLEAN NOT NULL DEFAULT TRUE, priority INTEGER NOT NULL DEFAULT 100,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_property_account_links_account ON property_account_links(account_id,active,property_id)`,
      `CREATE INDEX IF NOT EXISTS idx_property_allocations_transaction ON property_transaction_allocations(transaction_id,property_id)`,
      `CREATE INDEX IF NOT EXISTS idx_property_rules_household_active ON property_transaction_rules(household_id,active,priority)`,
    ],
  },
  {
    id: "0016_realtime_notification_jobs",
    description: "Verified Plaid webhook inbox, durable sync jobs, delivery lifecycle, and notification dismissal",
    statements: [
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS provider_item_id TEXT`,
      `ALTER TABLE alerts ADD COLUMN IF NOT EXISTS dismissed_at TIMESTAMPTZ`,
      `ALTER TABLE alert_deliveries ADD COLUMN IF NOT EXISTS available_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`,
      `CREATE TABLE IF NOT EXISTS background_jobs (
        id TEXT PRIMARY KEY, household_id TEXT REFERENCES households(id) ON DELETE CASCADE,
        job_type TEXT NOT NULL CHECK(job_type IN ('PLAID_SYNC','NOTIFICATION_DELIVERY')),
        idempotency_key TEXT NOT NULL UNIQUE, payload_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        status TEXT NOT NULL DEFAULT 'QUEUED' CHECK(status IN ('QUEUED','RUNNING','SUCCEEDED','FAILED','DEAD')),
        attempts INTEGER NOT NULL DEFAULT 0, available_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        locked_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, error_code TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS plaid_webhook_events (
        id TEXT PRIMARY KEY, item_id TEXT, webhook_type TEXT NOT NULL, webhook_code TEXT NOT NULL,
        request_hash TEXT NOT NULL UNIQUE, verified BOOLEAN NOT NULL, payload_json JSONB NOT NULL,
        received_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_background_jobs_due ON background_jobs(status,available_at,created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_connections_provider_item ON connections(provider,provider_item_id)`,
    ],
  },
  {
    id: "0017_pwa_push_devices",
    description: "Identify and safely manage multiple PWA push devices",
    statements: [
      `ALTER TABLE push_subscriptions ADD COLUMN IF NOT EXISTS device_name TEXT`,
      `ALTER TABLE push_subscriptions ADD COLUMN IF NOT EXISTS platform TEXT`,
      `ALTER TABLE push_subscriptions ADD COLUMN IF NOT EXISTS user_agent_hint TEXT`,
      `CREATE INDEX IF NOT EXISTS idx_push_subscriptions_user_active ON push_subscriptions(household_id,user_id,active,updated_at DESC)`,
    ],
  },
  {
    id: "0018_market_intelligence_alert_engine",
    description: "Server-driven market alert preferences, evidence ledger, deduplication, and worker jobs",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE'))`,
      `CREATE TABLE IF NOT EXISTS market_alert_preferences (
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        enabled BOOLEAN NOT NULL DEFAULT TRUE, channels_json JSONB NOT NULL DEFAULT '{"inApp":true,"push":false,"email":false}'::jsonb,
        severities_json JSONB NOT NULL DEFAULT '{"ACTION_NOW":true,"IMPORTANT":true,"WATCH":true,"INFORMATIONAL":false}'::jsonb,
        classes_json JSONB NOT NULL DEFAULT '{}'::jsonb, quiet_exceptions_json JSONB NOT NULL DEFAULT '{"ACTION_NOW":true}'::jsonb,
        digest_low_value_news BOOLEAN NOT NULL DEFAULT TRUE, max_immediate_per_hour INTEGER NOT NULL DEFAULT 8,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(household_id,user_id)
      )`,
      `CREATE TABLE IF NOT EXISTS symbol_alert_preferences (
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE, symbol TEXT NOT NULL,
        enabled BOOLEAN NOT NULL DEFAULT TRUE, timeframe TEXT NOT NULL DEFAULT '5–7 Year',
        price_above NUMERIC(20,6), price_below NUMERIC(20,6), support_level NUMERIC(20,6), resistance_level NUMERIC(20,6),
        move_percent NUMERIC(8,3) NOT NULL DEFAULT 4, volume_ratio NUMERIC(8,3) NOT NULL DEFAULT 1.8,
        minimum_confidence INTEGER NOT NULL DEFAULT 70 CHECK(minimum_confidence BETWEEN 0 AND 100),
        cooldown_minutes INTEGER NOT NULL DEFAULT 240, classes_json JSONB NOT NULL DEFAULT '{}', channels_json JSONB NOT NULL DEFAULT '{}',
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(household_id,user_id,symbol)
      )`,
      `CREATE TABLE IF NOT EXISTS market_intelligence_events (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        symbol TEXT, company_name TEXT, event_class TEXT NOT NULL, severity TEXT NOT NULL,
        fingerprint TEXT NOT NULL UNIQUE, title TEXT NOT NULL, explanation TEXT NOT NULL,
        evidence_json JSONB NOT NULL, confidence INTEGER NOT NULL CHECK(confidence BETWEEN 0 AND 100),
        source_as_of TIMESTAMPTZ NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_market_events_household_time ON market_intelligence_events(household_id,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_market_events_symbol_class ON market_intelligence_events(household_id,symbol,event_class,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_symbol_alert_preferences_symbol ON symbol_alert_preferences(household_id,symbol,enabled)`,
    ],
  },
  {
    id: "0019_transaction_alert_classes",
    description: "First-class transaction alert classes for configurable server-driven delivery",
    statements: [
      `ALTER TABLE transaction_notification_events DROP CONSTRAINT IF EXISTS transaction_notification_events_event_type_check`,
      `ALTER TABLE transaction_notification_events ADD CONSTRAINT transaction_notification_events_event_type_check CHECK(event_type IN ('IMPORTED','UPDATED','PENDING_POSTED','RECURRING_IDENTIFIED','DEPOSIT','WITHDRAWAL','CARD_PURCHASE','LARGE_TRANSACTION','NEW_MERCHANT','SUBSCRIPTION_INCREASE','ATM_WITHDRAWAL','FOREIGN_TRANSACTION','FEE','DUPLICATE_CHARGE','SUSPICIOUS','REMOVED'))`,
    ],
  },
  {
    id: "0020_notification_schema_drift_repair",
    description: "Repair notification columns for databases whose earlier migration ledger preceded the final schema",
    statements: [
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS provider_item_id TEXT`,
      `ALTER TABLE alerts ADD COLUMN IF NOT EXISTS dismissed_at TIMESTAMPTZ`,
      `ALTER TABLE alert_deliveries ADD COLUMN IF NOT EXISTS available_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`,
      `ALTER TABLE push_subscriptions ADD COLUMN IF NOT EXISTS device_name TEXT`,
      `ALTER TABLE push_subscriptions ADD COLUMN IF NOT EXISTS platform TEXT`,
      `ALTER TABLE push_subscriptions ADD COLUMN IF NOT EXISTS user_agent_hint TEXT`,
      `CREATE INDEX IF NOT EXISTS idx_connections_provider_item ON connections(provider,provider_item_id)`,
      `CREATE INDEX IF NOT EXISTS idx_push_subscriptions_user_active ON push_subscriptions(household_id,user_id,active,updated_at DESC)`,
    ],
  },
  {
    id: "0021_plaid_investment_access",
    description: "Plaid Investments consent, synchronization diagnostics, and product-specific status",
    statements: [
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS plaid_products_json JSONB NOT NULL DEFAULT '[]'::jsonb`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS plaid_consented_products_json JSONB NOT NULL DEFAULT '[]'::jsonb`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS plaid_billed_products_json JSONB NOT NULL DEFAULT '[]'::jsonb`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS plaid_consent_expiration_at TIMESTAMPTZ`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS investment_access_status TEXT NOT NULL DEFAULT 'UNKNOWN'`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS last_holdings_sync_at TIMESTAMPTZ`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS last_investment_transactions_sync_at TIMESTAMPTZ`,
      `ALTER TABLE connections ADD COLUMN IF NOT EXISTS latest_plaid_error_message TEXT`,
      `CREATE INDEX IF NOT EXISTS idx_investment_transactions_external ON investment_transactions(account_id,source,external_id)`,
    ],
  },
  {
    id: "0022_market_wide_discovery",
    description: "Independent market-wide candidate discovery runs, current theses, and immutable history",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY'))`,
      `CREATE TABLE IF NOT EXISTS market_discovery_runs (
        id TEXT PRIMARY KEY, status TEXT NOT NULL CHECK(status IN ('RUNNING','SUCCEEDED','FAILED')),
        universe_size INTEGER NOT NULL DEFAULT 0, screened_count INTEGER NOT NULL DEFAULT 0,
        enriched_count INTEGER NOT NULL DEFAULT 0, source_summary_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        error_code TEXT, started_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, completed_at TIMESTAMPTZ
      )`,
      `CREATE TABLE IF NOT EXISTS market_discovery_candidates (
        symbol TEXT PRIMARY KEY, company_name TEXT NOT NULL, exchange TEXT, sector TEXT, industry TEXT, ipo_date DATE,
        status TEXT NOT NULL CHECK(status IN ('DISCOVERED_TODAY','EARLY_WATCH','RESEARCH_NOW','POSSIBLE_BUY_SETUP','REJECTED')),
        strategy_fit TEXT NOT NULL CHECK(strategy_fit IN ('SWING','LONG_TERM','BOTH')),
        business_quality INTEGER NOT NULL, growth_acceleration INTEGER NOT NULL, catalyst INTEGER NOT NULL,
        valuation INTEGER NOT NULL, technical_setup INTEGER NOT NULL, risk INTEGER NOT NULL, discovery_confidence INTEGER NOT NULL,
        why_found TEXT NOT NULL, changed_recently_json JSONB NOT NULL DEFAULT '[]'::jsonb,
        metrics_json JSONB NOT NULL DEFAULT '{}'::jsonb, catalyst_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        valuation_json JSONB NOT NULL DEFAULT '{}'::jsonb, technical_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        risks_json JSONB NOT NULL DEFAULT '[]'::jsonb, evidence_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        rejected_reason TEXT, first_detected_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        last_detected_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, source_as_of TIMESTAMPTZ NOT NULL,
        scan_run_id TEXT REFERENCES market_discovery_runs(id) ON DELETE SET NULL, model_version TEXT NOT NULL
      )`,
      `CREATE TABLE IF NOT EXISTS market_discovery_history (
        id TEXT PRIMARY KEY, symbol TEXT NOT NULL, scan_run_id TEXT REFERENCES market_discovery_runs(id) ON DELETE SET NULL,
        status TEXT NOT NULL, strategy_fit TEXT NOT NULL, scores_json JSONB NOT NULL, thesis TEXT NOT NULL,
        evidence_json JSONB NOT NULL, source_as_of TIMESTAMPTZ NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_discovery_status_confidence ON market_discovery_candidates(status,discovery_confidence DESC,last_detected_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_discovery_ipo ON market_discovery_candidates(ipo_date DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_discovery_history_symbol ON market_discovery_history(symbol,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_discovery_runs_time ON market_discovery_runs(started_at DESC)`,
    ],
  },
  {
    id: "0023_daily_market_close_reviews",
    description: "Server-generated close reviews, next-session scenario forecasts, immutable revisions, and calibration outcomes",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH'))`,
      `CREATE TABLE IF NOT EXISTS daily_market_reviews (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        session_date DATE NOT NULL, next_session_date DATE NOT NULL, revision INTEGER NOT NULL DEFAULT 1,
        review_kind TEXT NOT NULL CHECK(review_kind IN ('MARKET_CLOSE','OVERNIGHT_REVISION','PREMARKET_REVISION')),
        status TEXT NOT NULL DEFAULT 'READY' CHECK(status IN ('BUILDING','READY','FAILED')),
        market_summary_json JSONB NOT NULL, portfolio_summary_json JSONB NOT NULL, ticker_statuses_json JSONB NOT NULL,
        changes_json JSONB NOT NULL, confirmed_signals_json JSONB NOT NULL, invalidated_signals_json JSONB NOT NULL,
        overnight_events_json JSONB NOT NULL, watchlist_json JSONB NOT NULL, prepared_orders_json JSONB NOT NULL,
        scenarios_json JSONB NOT NULL, risk_summary_json JSONB NOT NULL, source_snapshot_json JSONB NOT NULL,
        material_change_reason TEXT, parent_review_id TEXT REFERENCES daily_market_reviews(id) ON DELETE SET NULL,
        generated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, source_as_of TIMESTAMPTZ NOT NULL,
        UNIQUE(household_id,session_date,revision)
      )`,
      `CREATE TABLE IF NOT EXISTS daily_market_review_outcomes (
        id TEXT PRIMARY KEY, review_id TEXT NOT NULL REFERENCES daily_market_reviews(id) ON DELETE CASCADE,
        evaluated_session_date DATE NOT NULL, actual_scenario TEXT NOT NULL, scenario_match BOOLEAN,
        brier_score NUMERIC(10,6), false_positive_count INTEGER NOT NULL DEFAULT 0,
        outcome_json JSONB NOT NULL, evaluated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(review_id,evaluated_session_date)
      )`,
      `CREATE TABLE IF NOT EXISTS market_monitor_state (
        monitor_key TEXT PRIMARY KEY, last_processed_at TIMESTAMPTZ, last_event_cursor TEXT,
        trigger_state_json JSONB NOT NULL DEFAULT '{}'::jsonb, last_success_at TIMESTAMPTZ,
        latest_error_code TEXT, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS worker_heartbeats (
        worker_name TEXT PRIMARY KEY, status TEXT NOT NULL, market_session TEXT, metadata_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        jobs_succeeded INTEGER NOT NULL DEFAULT 0, jobs_failed INTEGER NOT NULL DEFAULT 0,
        heartbeat_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_daily_reviews_household_date ON daily_market_reviews(household_id,session_date DESC,revision DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_daily_review_outcomes_review ON daily_market_review_outcomes(review_id,evaluated_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_worker_heartbeats_time ON worker_heartbeats(heartbeat_at DESC)`,
    ],
  },
  {
    id: "0024_tactical_sell_rebuy",
    description: "Tax-aware tactical trim proposals, durable re-entry monitoring, and hold-benchmark outcomes",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR'))`,
      `CREATE TABLE IF NOT EXISTS tactical_rebuy_plans (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE, security_id TEXT NOT NULL REFERENCES securities(id),
        status TEXT NOT NULL CHECK(status IN ('PROPOSED','PREPARE_TO_TRIM','AWAITING_EXECUTION','MONITORING_REENTRY','REENTRY_READY','INVALIDATED_UPWARD','REENTERED','CANCELLED','EXPIRED')),
        action TEXT NOT NULL CHECK(action IN ('HOLD','TRIM_REBUY','TACTICAL_EXIT_REENTRY')),
        current_shares NUMERIC(28,8) NOT NULL, trim_shares NUMERIC(28,8) NOT NULL, keep_shares NUMERIC(28,8) NOT NULL,
        reference_price_cents BIGINT NOT NULL, trigger_low_cents BIGINT, trigger_high_cents BIGINT,
        expected_pullback_low_cents BIGINT NOT NULL, expected_pullback_high_cents BIGINT NOT NULL,
        rebuy_low_cents BIGINT NOT NULL, rebuy_high_cents BIGINT NOT NULL, upward_invalidation_cents BIGINT NOT NULL,
        estimated_proceeds_cents BIGINT NOT NULL, cost_basis_cents BIGINT, estimated_realized_pnl_cents BIGINT,
        estimated_tax_cents BIGINT NOT NULL DEFAULT 0, estimated_costs_cents BIGINT NOT NULL DEFAULT 0,
        expected_net_benefit_cents BIGINT NOT NULL, expected_share_improvement NUMERIC(28,8) NOT NULL DEFAULT 0,
        pullback_probability INTEGER NOT NULL, continuation_probability INTEGER NOT NULL,
        confirmation_groups_json JSONB NOT NULL, scenarios_json JSONB NOT NULL, evidence_json JSONB NOT NULL,
        why_not_hold TEXT NOT NULL, why_not_sell_all TEXT NOT NULL, failure_case TEXT NOT NULL,
        exit_transaction_id TEXT REFERENCES investment_transactions(id), actual_exit_price_cents BIGINT, actual_exit_proceeds_cents BIGINT,
        rebuy_transaction_id TEXT REFERENCES investment_transactions(id), actual_rebuy_price_cents BIGINT,
        first_alerted_at TIMESTAMPTZ, last_evaluated_at TIMESTAMPTZ, expires_at TIMESTAMPTZ NOT NULL,
        model_version TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS tactical_rebuy_outcomes (
        id TEXT PRIMARY KEY, plan_id TEXT NOT NULL UNIQUE REFERENCES tactical_rebuy_plans(id) ON DELETE CASCADE,
        shares_gained NUMERIC(28,8), net_dollars_saved_cents BIGINT, missed_upside_cents BIGINT,
        hold_value_cents BIGINT, tactical_value_cents BIGINT, excess_value_vs_hold_cents BIGINT,
        outcome_json JSONB NOT NULL, evaluated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS tactical_symbol_calibration (
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE, symbol TEXT NOT NULL,
        completed_plans INTEGER NOT NULL DEFAULT 0, wins_vs_hold INTEGER NOT NULL DEFAULT 0,
        cumulative_excess_cents BIGINT NOT NULL DEFAULT 0, required_confirmation_groups INTEGER NOT NULL DEFAULT 4,
        minimum_advantage_bps INTEGER NOT NULL DEFAULT 75, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY(household_id,symbol)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_tactical_plans_monitor ON tactical_rebuy_plans(status,last_evaluated_at,expires_at)`,
      `CREATE INDEX IF NOT EXISTS idx_tactical_plans_account_symbol ON tactical_rebuy_plans(account_id,security_id,created_at DESC)`,
    ],
  },
  {
    id: "0025_ai_decision_engine_options",
    description: "Versioned central decision runs, controlled model evaluation, immutable outcomes, and options positions",
    statements: [
      `CREATE TABLE IF NOT EXISTS ai_model_versions (
        version TEXT NOT NULL, strategy TEXT NOT NULL, status TEXT NOT NULL CHECK(status IN ('CHAMPION','CHALLENGER','RETIRED','ROLLED_BACK')),
        provider TEXT NOT NULL, prompt_hash TEXT NOT NULL, strategy_version TEXT NOT NULL,
        training_window_json JSONB NOT NULL DEFAULT '{}'::jsonb, validation_results_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        safety_gates_json JSONB NOT NULL DEFAULT '{}'::jsonb, calibration_factor NUMERIC(8,6) NOT NULL DEFAULT 0.85,
        released_at TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY(version,strategy)
      )`,
      `CREATE TABLE IF NOT EXISTS ai_decision_runs (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE, ticker TEXT NOT NULL,
        strategy TEXT NOT NULL, request_type TEXT NOT NULL, input_snapshot_json JSONB NOT NULL,
        evidence_json JSONB NOT NULL, output_json JSONB NOT NULL, data_quality INTEGER NOT NULL CHECK(data_quality BETWEEN 0 AND 100),
        model_version TEXT NOT NULL, strategy_version TEXT NOT NULL, provider TEXT NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('COMPLETED','FAILED','REJECTED_BY_GATE')),
        latency_ms INTEGER NOT NULL, data_timestamp TIMESTAMPTZ NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS ai_decision_outcomes (
        id TEXT PRIMARY KEY, decision_id TEXT NOT NULL UNIQUE REFERENCES ai_decision_runs(id) ON DELETE CASCADE,
        evaluation_horizon TEXT NOT NULL, actual_return_bps INTEGER, alternative_return_bps INTEGER,
        max_adverse_excursion_bps INTEGER, max_favorable_excursion_bps INTEGER, drawdown_bps INTEGER,
        direction_correct BOOLEAN, outperformed_alternative BOOLEAN, false_positive BOOLEAN,
        outcome_json JSONB NOT NULL, evaluated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS ai_model_evaluation_runs (
        id TEXT PRIMARY KEY, model_version TEXT NOT NULL, strategy TEXT NOT NULL,
        evaluation_type TEXT NOT NULL CHECK(evaluation_type IN ('BACKTEST','WALK_FORWARD','OUT_OF_SAMPLE','CALIBRATION')),
        window_start TIMESTAMPTZ NOT NULL, window_end TIMESTAMPTZ NOT NULL, no_future_leakage BOOLEAN NOT NULL DEFAULT FALSE,
        metrics_json JSONB NOT NULL, gates_json JSONB NOT NULL, passed BOOLEAN NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS option_positions (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE, underlying_symbol TEXT NOT NULL,
        contract_symbol TEXT NOT NULL, option_type TEXT NOT NULL CHECK(option_type IN ('CALL','PUT')),
        expiration DATE NOT NULL, strike_cents BIGINT NOT NULL, quantity INTEGER NOT NULL CHECK(quantity>0),
        entry_premium_cents BIGINT NOT NULL, current_premium_cents BIGINT, underlying_entry_cents BIGINT NOT NULL,
        underlying_current_cents BIGINT, thesis TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'OPEN' CHECK(status IN ('OPEN','CLOSED','EXPIRED','INVALIDATED')),
        target_premium_cents BIGINT, stop_premium_cents BIGINT, underlying_invalidation_cents BIGINT,
        greeks_json JSONB NOT NULL DEFAULT '{}'::jsonb, evidence_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        opened_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, closed_at TIMESTAMPTZ, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_ai_decisions_account_time ON ai_decision_runs(account_id,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_ai_decisions_model_strategy ON ai_decision_runs(model_version,strategy,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_option_positions_monitor ON option_positions(status,expiration,updated_at)`,
    ],
  },
  {
    id: "0026_market_discovery_operations",
    description: "Operational metrics and AI audit fields for market-wide candidate discovery",
    statements: [
      `ALTER TABLE market_discovery_runs ADD COLUMN IF NOT EXISTS rejected_count INTEGER NOT NULL DEFAULT 0`,
      `ALTER TABLE market_discovery_runs ADD COLUMN IF NOT EXISTS candidates_created INTEGER NOT NULL DEFAULT 0`,
      `ALTER TABLE market_discovery_runs ADD COLUMN IF NOT EXISTS provider_errors_json JSONB NOT NULL DEFAULT '[]'::jsonb`,
      `ALTER TABLE market_discovery_runs ADD COLUMN IF NOT EXISTS ai_analysis_failures INTEGER NOT NULL DEFAULT 0`,
      `ALTER TABLE market_discovery_candidates ADD COLUMN IF NOT EXISTS ai_decision_json JSONB NOT NULL DEFAULT '{}'::jsonb`,
    ],
  },
  {
    id: "0027_institutional_options_flow",
    description: "Raw institutional options flow, transparent Northstar classifications, summaries, and server monitoring",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW'))`,
      `CREATE TABLE IF NOT EXISTS options_flow_events (
        id TEXT PRIMARY KEY, provider_event_id TEXT NOT NULL, provider TEXT NOT NULL, symbol TEXT NOT NULL,
        contract_symbol TEXT, option_type TEXT CHECK(option_type IN ('CALL','PUT')), strike_cents BIGINT,
        expiration DATE, occurred_at TIMESTAMPTZ NOT NULL, underlying_price_cents BIGINT,
        trade_price_cents BIGINT, bid_cents BIGINT, ask_cents BIGINT, contracts INTEGER,
        premium_cents BIGINT, volume INTEGER, open_interest INTEGER, volume_oi_ratio NUMERIC(16,6),
        implied_volatility NUMERIC(16,8), delta NUMERIC(16,8), gamma NUMERIC(16,8),
        theta NUMERIC(16,8), vega NUMERIC(16,8), exchange_routes_json JSONB NOT NULL DEFAULT '[]'::jsonb,
        raw_json JSONB NOT NULL, provider_classification TEXT, northstar_classification TEXT NOT NULL,
        direction TEXT NOT NULL CHECK(direction IN ('BULLISH','BEARISH','NEUTRAL','UNKNOWN')),
        opening_likelihood TEXT NOT NULL, execution_aggression TEXT NOT NULL,
        conviction_score INTEGER NOT NULL CHECK(conviction_score BETWEEN 0 AND 100),
        reasons_json JSONB NOT NULL, next_day_oi_confirmed BOOLEAN, cluster_key TEXT,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(provider,provider_event_id)
      )`,
      `CREATE TABLE IF NOT EXISTS options_flow_summaries (
        symbol TEXT PRIMARY KEY, source_mode TEXT NOT NULL, net_call_premium_cents BIGINT NOT NULL DEFAULT 0,
        net_put_premium_cents BIGINT NOT NULL DEFAULT 0, bullish_premium_cents BIGINT NOT NULL DEFAULT 0,
        bearish_premium_cents BIGINT NOT NULL DEFAULT 0, sweep_count INTEGER NOT NULL DEFAULT 0,
        high_conviction_count INTEGER NOT NULL DEFAULT 0, largest_trades_json JSONB NOT NULL DEFAULT '[]'::jsonb,
        timeline_json JSONB NOT NULL DEFAULT '[]'::jsonb, dark_pool_levels_json JSONB NOT NULL DEFAULT '[]'::jsonb,
        gex_json JSONB NOT NULL DEFAULT '{}'::jsonb, call_wall_cents BIGINT, put_wall_cents BIGINT,
        zero_gamma_cents BIGINT, flow_trend TEXT NOT NULL DEFAULT 'INSUFFICIENT_DATA',
        institutional_conviction INTEGER NOT NULL DEFAULT 0, provider_status TEXT NOT NULL,
        provider_error TEXT, data_as_of TIMESTAMPTZ, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS options_flow_settings (
        id TEXT PRIMARY KEY DEFAULT 'global', golden_min_premium_cents BIGINT NOT NULL DEFAULT 100000000,
        golden_min_contracts INTEGER NOT NULL DEFAULT 500, golden_min_volume_oi NUMERIC(10,4) NOT NULL DEFAULT 1.5,
        high_conviction_threshold INTEGER NOT NULL DEFAULT 75, alert_threshold INTEGER NOT NULL DEFAULT 82,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `INSERT INTO options_flow_settings(id) VALUES('global') ON CONFLICT(id) DO NOTHING`,
      `CREATE INDEX IF NOT EXISTS idx_options_flow_symbol_time ON options_flow_events(symbol,occurred_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_options_flow_conviction ON options_flow_events(conviction_score DESC,occurred_at DESC)`,
    ],
  },
  {
    id: "0028_plaid_investment_reconciliation",
    description: "Provider-confirmed investment refresh lifecycle, account freshness, webhook health, and reconciliation history",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW'))`,
      `ALTER TABLE accounts ADD COLUMN IF NOT EXISTS last_investment_sync_at TIMESTAMPTZ`,
      `ALTER TABLE accounts ADD COLUMN IF NOT EXISTS last_provider_update_at TIMESTAMPTZ`,
      `ALTER TABLE accounts ADD COLUMN IF NOT EXISTS investment_sync_status TEXT NOT NULL DEFAULT 'UNKNOWN'`,
      `ALTER TABLE accounts ADD COLUMN IF NOT EXISTS investment_sync_error TEXT`,
      `ALTER TABLE holdings ADD COLUMN IF NOT EXISTS provider_holding_id TEXT`,
      `ALTER TABLE holdings ADD COLUMN IF NOT EXISTS provider_account_id TEXT`,
      `ALTER TABLE holdings ADD COLUMN IF NOT EXISTS provider_security_id TEXT`,
      `ALTER TABLE holdings ADD COLUMN IF NOT EXISTS provider_updated_at TIMESTAMPTZ`,
      `ALTER TABLE securities ADD COLUMN IF NOT EXISTS provider_security_id TEXT`,
      `ALTER TABLE investment_transactions ADD COLUMN IF NOT EXISTS provider_account_id TEXT`,
      `ALTER TABLE investment_transactions ADD COLUMN IF NOT EXISTS provider_security_id TEXT`,
      `ALTER TABLE plaid_webhook_events ADD COLUMN IF NOT EXISTS processed_at TIMESTAMPTZ`,
      `ALTER TABLE plaid_webhook_events ADD COLUMN IF NOT EXISTS processing_error TEXT`,
      `CREATE TABLE IF NOT EXISTS investment_sync_history (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        connection_id TEXT NOT NULL REFERENCES connections(id) ON DELETE CASCADE, account_id TEXT REFERENCES accounts(id) ON DELETE CASCADE,
        trigger TEXT NOT NULL, status TEXT NOT NULL CHECK(status IN ('REFRESH_REQUESTED','WAITING_PROVIDER','SUCCEEDED','NO_CHANGES','FAILED')),
        provider_request_id TEXT, holdings_added INTEGER NOT NULL DEFAULT 0, holdings_changed INTEGER NOT NULL DEFAULT 0,
        holdings_closed INTEGER NOT NULL DEFAULT 0, transactions_added INTEGER NOT NULL DEFAULT 0,
        result_json JSONB NOT NULL DEFAULT '{}'::jsonb, error_code TEXT, error_message TEXT,
        started_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, completed_at TIMESTAMPTZ
      )`,
      `CREATE UNIQUE INDEX IF NOT EXISTS idx_holdings_provider_identity ON holdings(account_id,provider_security_id) WHERE provider_security_id IS NOT NULL`,
      `CREATE INDEX IF NOT EXISTS idx_investment_sync_history_connection ON investment_sync_history(connection_id,started_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_plaid_webhook_investment_health ON plaid_webhook_events(webhook_type,webhook_code,received_at DESC)`,
    ],
  },
  {
    id: "0029_investment_notification_reliability",
    description: "Account-specific notification coverage, auditability, suppression reasons, and pipeline tests",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT'))`,
      `CREATE TABLE IF NOT EXISTS investment_notification_coverage (
        account_id TEXT PRIMARY KEY REFERENCES accounts(id) ON DELETE CASCADE,
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        strategy TEXT NOT NULL, status TEXT NOT NULL CHECK(status IN ('HEALTHY','STALE','DEGRADED','ERROR','NOT_CONFIGURED')),
        positions_monitored INTEGER NOT NULL DEFAULT 0, watchlist_symbols_monitored INTEGER NOT NULL DEFAULT 0,
        option_contracts_monitored INTEGER NOT NULL DEFAULT 0, price_rules_active INTEGER NOT NULL DEFAULT 0,
        recommendation_rules_active INTEGER NOT NULL DEFAULT 0, unmapped_positions INTEGER NOT NULL DEFAULT 0,
        last_provider_sync_at TIMESTAMPTZ, last_market_data_at TIMESTAMPTZ, last_ai_analysis_at TIMESTAMPTZ,
        last_recommendation_at TIMESTAMPTZ, last_alert_evaluation_at TIMESTAMPTZ, last_notification_at TIMESTAMPTZ,
        last_push_at TIMESTAMPTZ, last_email_at TIMESTAMPTZ, last_in_app_at TIMESTAMPTZ,
        worker_status TEXT NOT NULL DEFAULT 'UNKNOWN', error_code TEXT, details_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        heartbeat_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS investment_notification_suppressions (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        account_id TEXT REFERENCES accounts(id) ON DELETE CASCADE, ticker TEXT, event_type TEXT NOT NULL,
        fingerprint TEXT NOT NULL, reason TEXT NOT NULL CHECK(reason IN ('DUPLICATE','BELOW_THRESHOLD','QUIET_HOURS','USER_DISABLED','STALE_DATA','INSUFFICIENT_CONFIRMATION')),
        detail TEXT, data_timestamp TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS investment_notification_preferences (
        household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE, user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE, event_type TEXT NOT NULL DEFAULT '*', enabled BOOLEAN NOT NULL DEFAULT TRUE,
        channels_json JSONB NOT NULL DEFAULT '{"inApp":true,"push":true,"email":true}'::jsonb,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(household_id,user_id,account_id,event_type)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_investment_coverage_household ON investment_notification_coverage(household_id,status,heartbeat_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_investment_suppressions_search ON investment_notification_suppressions(household_id,account_id,ticker,event_type,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_alert_delivery_audit ON alert_deliveries(user_id,created_at DESC,status)`,
    ],
  },  {
    id: "0030_continuous_intelligence_loop",
    description: "Persisted account-first Sync Analyze Predict Recommend Alert Measure Learn loop",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT','ACCOUNT_INTELLIGENCE_LOOP'))`,
      `CREATE TABLE IF NOT EXISTS intelligence_loop_runs (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE, strategy TEXT NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('QUEUED','RUNNING','SUCCEEDED','DEGRADED','FAILED')),
        trigger TEXT NOT NULL, cycle_key TEXT NOT NULL UNIQUE, data_timestamp TIMESTAMPTZ,
        model_version TEXT, strategy_version TEXT, positions_loaded INTEGER NOT NULL DEFAULT 0,
        decisions_created INTEGER NOT NULL DEFAULT 0, recommendations_evaluated INTEGER NOT NULL DEFAULT 0,
        alerts_created INTEGER NOT NULL DEFAULT 0, outcomes_measured INTEGER NOT NULL DEFAULT 0,
        error_code TEXT, summary_json JSONB NOT NULL DEFAULT '{}'::jsonb,
        started_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, completed_at TIMESTAMPTZ,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS intelligence_loop_stages (
        id TEXT PRIMARY KEY, run_id TEXT NOT NULL REFERENCES intelligence_loop_runs(id) ON DELETE CASCADE,
        stage TEXT NOT NULL CHECK(stage IN ('SYNC','ANALYZE','PREDICT','RECOMMEND','ALERT','MEASURE','LEARN','REANALYZE')),
        status TEXT NOT NULL CHECK(status IN ('PENDING','RUNNING','SUCCEEDED','SKIPPED','DEGRADED','FAILED')),
        input_count INTEGER NOT NULL DEFAULT 0, output_count INTEGER NOT NULL DEFAULT 0,
        evidence_json JSONB NOT NULL DEFAULT '{}'::jsonb, error_code TEXT,
        started_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, completed_at TIMESTAMPTZ,
        UNIQUE(run_id,stage)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_intelligence_loop_account_time ON intelligence_loop_runs(account_id,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_intelligence_loop_household_status ON intelligence_loop_runs(household_id,status,created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_intelligence_loop_stage_run ON intelligence_loop_stages(run_id,stage)`,
    ],
  },  {
    id: "0031_kids_future_wealth_planning",
    description: "Independent child profiles, goals, linked accounts, deterministic projections, contributions, reviews, and alerts",
    statements: [
      `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
      `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT','ACCOUNT_INTELLIGENCE_LOOP','KIDS_PLAN_REVIEW'))`,
      `CREATE TABLE IF NOT EXISTS child_profiles (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        nickname TEXT NOT NULL, birth_date DATE, birth_year INTEGER, expected_college_start_year INTEGER,
        expected_college_duration_years INTEGER NOT NULL DEFAULT 4, planning_mode TEXT NOT NULL CHECK(planning_mode IN ('EDUCATION','WEALTH','BOTH')),
        risk_preference TEXT NOT NULL CHECK(risk_preference IN ('CONSERVATIVE','BALANCED','GROWTH','AGGRESSIVE')),
        monthly_contribution_capacity_cents BIGINT NOT NULL DEFAULT 0, expected_family_contributions_cents BIGINT NOT NULL DEFAULT 0,
        avatar_color TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS child_goals (
        id TEXT PRIMARY KEY, child_id TEXT NOT NULL REFERENCES child_profiles(id) ON DELETE CASCADE,
        goal_type TEXT NOT NULL CHECK(goal_type IN ('EDUCATION','FUTURE_WEALTH')), name TEXT NOT NULL,
        target_amount_cents BIGINT NOT NULL, target_date DATE, target_age INTEGER, current_manual_balance_cents BIGINT NOT NULL DEFAULT 0,
        monthly_contribution_cents BIGINT NOT NULL DEFAULT 0, minimum_monthly_cents BIGINT NOT NULL DEFAULT 0,
        priority INTEGER NOT NULL DEFAULT 50 CHECK(priority BETWEEN 0 AND 100), education_cost_type TEXT,
        current_annual_cost_cents BIGINT, education_inflation_bps INTEGER NOT NULL DEFAULT 500,
        return_conservative_bps INTEGER NOT NULL DEFAULT 500, return_base_bps INTEGER NOT NULL DEFAULT 700, return_aggressive_bps INTEGER NOT NULL DEFAULT 900,
        satellite_allocation_bps INTEGER NOT NULL DEFAULT 0 CHECK(satellite_allocation_bps BETWEEN 0 AND 2000),
        status TEXT NOT NULL DEFAULT 'ACTIVE' CHECK(status IN ('ACTIVE','PAUSED','FUNDED','WITHDRAWING','COMPLETED')),
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS child_goal_accounts (
        goal_id TEXT NOT NULL REFERENCES child_goals(id) ON DELETE CASCADE, account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        account_structure TEXT NOT NULL CHECK(account_structure IN ('529','UTMA_UGMA','PARENT_TAXABLE','CUSTODIAL_ROTH_IRA','SAVINGS','OTHER')),
        allocation_percent INTEGER NOT NULL DEFAULT 100 CHECK(allocation_percent BETWEEN 0 AND 100), linked_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY(goal_id,account_id)
      )`,
      `CREATE TABLE IF NOT EXISTS child_plan_projections (
        id TEXT PRIMARY KEY, child_id TEXT NOT NULL REFERENCES child_profiles(id) ON DELETE CASCADE, goal_id TEXT NOT NULL REFERENCES child_goals(id) ON DELETE CASCADE,
        assumption_version TEXT NOT NULL, source_date DATE NOT NULL, current_balance_cents BIGINT NOT NULL,
        conservative_balance_cents BIGINT NOT NULL, base_balance_cents BIGINT NOT NULL, aggressive_balance_cents BIGINT NOT NULL,
        required_monthly_conservative_cents BIGINT NOT NULL, required_monthly_base_cents BIGINT NOT NULL, required_monthly_aggressive_cents BIGINT NOT NULL,
        required_lump_sum_base_cents BIGINT NOT NULL, funding_percent INTEGER NOT NULL, funding_status TEXT NOT NULL CHECK(funding_status IN ('AHEAD','ON_TRACK','BEHIND')),
        contribution_principal_cents BIGINT NOT NULL, projected_growth_cents BIGINT NOT NULL, funding_gap_cents BIGINT NOT NULL,
        assumptions_json JSONB NOT NULL, calculated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS child_contributions (
        id TEXT PRIMARY KEY, child_id TEXT NOT NULL REFERENCES child_profiles(id) ON DELETE CASCADE, goal_id TEXT NOT NULL REFERENCES child_goals(id) ON DELETE CASCADE,
        account_id TEXT REFERENCES accounts(id) ON DELETE SET NULL, investment_transaction_id TEXT REFERENCES investment_transactions(id) ON DELETE SET NULL,
        amount_cents BIGINT NOT NULL, expected_cents BIGINT, contribution_date DATE NOT NULL, source TEXT NOT NULL CHECK(source IN ('DETECTED','MANUAL','FAMILY','TRANSFER')),
        status TEXT NOT NULL CHECK(status IN ('EXPECTED','RECEIVED','MISSED')), created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS child_plan_reviews (
        id TEXT PRIMARY KEY, child_id TEXT NOT NULL REFERENCES child_profiles(id) ON DELETE CASCADE,
        review_type TEXT NOT NULL CHECK(review_type IN ('MONTHLY','ANNUAL','ON_DEMAND','RISK_GUARDRAIL')), status TEXT NOT NULL,
        summary_json JSONB NOT NULL, assumptions_version TEXT NOT NULL, source_date DATE NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS child_plan_recommendations (
        id TEXT PRIMARY KEY, child_id TEXT NOT NULL REFERENCES child_profiles(id) ON DELETE CASCADE, goal_id TEXT REFERENCES child_goals(id) ON DELETE CASCADE,
        account_id TEXT REFERENCES accounts(id) ON DELETE SET NULL, action TEXT NOT NULL, amount_cents BIGINT,
        explanation TEXT NOT NULL, impact_json JSONB NOT NULL, confidence INTEGER NOT NULL, data_timestamp TIMESTAMPTZ NOT NULL,
        status TEXT NOT NULL DEFAULT 'ACTIVE', created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_child_profiles_household ON child_profiles(household_id,created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_child_goals_child ON child_goals(child_id,goal_type,status)`,
      `CREATE INDEX IF NOT EXISTS idx_child_projections_goal_time ON child_plan_projections(goal_id,calculated_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_child_contributions_goal_date ON child_contributions(goal_id,contribution_date DESC)`,
    ],
  },
  {
    id: "0032_fidelity_manual_and_plaid_diagnostics",
    description: "Fidelity manual ownership, cash, holding acquisition dates, and durable Plaid connection diagnostics",
    statements: [
      `ALTER TABLE accounts ADD COLUMN IF NOT EXISTS manual_owner_name TEXT`,
      `ALTER TABLE holdings ADD COLUMN IF NOT EXISTS acquisition_date DATE`,
      `CREATE INDEX IF NOT EXISTS idx_audit_plaid_connection_issue ON audit_log(household_id,action,created_at DESC)`,
    ],
  },
  {
    id: "0033_notification_timestamp_consistency",
    description: "Normalize alert lifecycle timestamps to timestamptz and remove legacy text/timestamp ambiguity",
    statements: [
      `ALTER TABLE alerts ALTER COLUMN read_at TYPE TIMESTAMPTZ USING NULLIF(BTRIM(read_at::text),'')::timestamptz`,
      `ALTER TABLE alerts ALTER COLUMN created_at DROP DEFAULT`,
      `ALTER TABLE alerts ALTER COLUMN created_at TYPE TIMESTAMPTZ USING COALESCE(NULLIF(BTRIM(created_at::text),'')::timestamptz,CURRENT_TIMESTAMP)`,
      `ALTER TABLE alerts ALTER COLUMN created_at SET DEFAULT CURRENT_TIMESTAMP`,
      `ALTER TABLE alerts ALTER COLUMN created_at SET NOT NULL`,
      `ALTER TABLE alerts ALTER COLUMN dismissed_at TYPE TIMESTAMPTZ USING NULLIF(BTRIM(dismissed_at::text),'')::timestamptz`,
      `CREATE INDEX IF NOT EXISTS idx_alerts_household_active_unread ON alerts(household_id,created_at DESC) WHERE dismissed_at IS NULL AND read_at IS NULL`,
    ],
  },
  {
    id: "0034_authoritative_recommendations",
    description: "One versioned active account-specific recommendation per security with snapshot lineage and supersession",
    statements: [
      `ALTER TABLE recommendations DROP CONSTRAINT IF EXISTS recommendations_lifecycle_check`,
      `ALTER TABLE recommendations ADD CONSTRAINT recommendations_lifecycle_check CHECK(lifecycle IN ('MONITORING','TRIGGERED','EXPIRED','CANCELLED','INVALIDATED','SUPERSEDED','COMPLETED','MISSED'))`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS strategy_version TEXT`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS research_snapshot_id TEXT`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS market_snapshot_id TEXT`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS generated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS superseded_by TEXT REFERENCES recommendations(id)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS idx_one_active_recommendation_per_account_security ON recommendations(account_id,security_id) WHERE lifecycle IN ('MONITORING','TRIGGERED')`,
    ],
  },
  {
    id: "0035_discovery_diversity_scores",
    description: "Independent discovery categories, crowding risk, diversified scorecard, and market coverage evidence",
    statements: [
      `ALTER TABLE market_discovery_candidates ADD COLUMN IF NOT EXISTS discovery_category TEXT`,
      `ALTER TABLE market_discovery_candidates ADD COLUMN IF NOT EXISTS cap_bucket TEXT`,
      `ALTER TABLE market_discovery_candidates ADD COLUMN IF NOT EXISTS hotness_crowding INTEGER NOT NULL DEFAULT 0 CHECK(hotness_crowding BETWEEN 0 AND 100)`,
      `ALTER TABLE market_discovery_candidates ADD COLUMN IF NOT EXISTS scores_json JSONB NOT NULL DEFAULT '{}'::jsonb`,
      `ALTER TABLE market_discovery_candidates ADD COLUMN IF NOT EXISTS underfollowed_reason TEXT`,
      `ALTER TABLE market_discovery_runs ADD COLUMN IF NOT EXISTS coverage_json JSONB NOT NULL DEFAULT '{}'::jsonb`,
      `ALTER TABLE market_discovery_runs ADD COLUMN IF NOT EXISTS concentration_warning TEXT`,
      `CREATE INDEX IF NOT EXISTS idx_discovery_category_confidence ON market_discovery_candidates(discovery_category,discovery_confidence DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_discovery_cap_bucket ON market_discovery_candidates(cap_bucket,discovery_confidence DESC)`,
    ],
  },
  {
    id: "0036_portfolio_builder_goals",
    description: "Confirmed account-specific goal profiles, feasibility, construction, stress tests, and monitoring",
    statements: [
      `CREATE TABLE IF NOT EXISTS portfolio_goal_profiles (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        investment_account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        goal_name TEXT NOT NULL, beneficiary TEXT, strategy_type TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'DRAFT' CHECK(status IN ('DRAFT','CONFIRMED','ACTIVE','PAUSED','COMPLETED')),
        target_amount_cents BIGINT NOT NULL, target_date DATE NOT NULL, current_capital_cents BIGINT NOT NULL DEFAULT 0,
        monthly_contribution_cents BIGINT NOT NULL DEFAULT 0, annual_lump_sum_cents BIGINT NOT NULL DEFAULT 0,
        risk_tolerance TEXT NOT NULL, risk_capacity TEXT NOT NULL, max_drawdown_bps INTEGER NOT NULL,
        liquidity_needs_cents BIGINT NOT NULL DEFAULT 0, minimum_cash_reserve_cents BIGINT NOT NULL DEFAULT 0,
        account_constraints_json JSONB NOT NULL DEFAULT '{}'::jsonb, assumptions_json JSONB NOT NULL,
        allocation_json JSONB NOT NULL, securities_json JSONB NOT NULL, feasibility_json JSONB NOT NULL,
        stress_tests_json JSONB NOT NULL, monitoring_json JSONB NOT NULL, source_description TEXT,
        confirmed_at TIMESTAMPTZ, last_analyzed_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_portfolio_goals_account ON portfolio_goal_profiles(investment_account_id,status,updated_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_portfolio_goals_household ON portfolio_goal_profiles(household_id,status,updated_at DESC)`,
    ],
  },
  {
    id: "0037_long_term_portfolio_balance",
    description: "Persist account target policies and whole-portfolio long-term balance reviews",
    statements: [
      `CREATE TABLE IF NOT EXISTS long_term_portfolio_policies (account_id TEXT PRIMARY KEY REFERENCES accounts(id) ON DELETE CASCADE,household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,model TEXT NOT NULL,target_allocation_json JSONB NOT NULL,tolerance_json JSONB NOT NULL,limits_json JSONB NOT NULL,contribution_rules_json JSONB NOT NULL,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `CREATE TABLE IF NOT EXISTS long_term_portfolio_reviews (id TEXT PRIMARY KEY,account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,policy_json JSONB NOT NULL,metrics_json JSONB NOT NULL,overlap_json JSONB NOT NULL,actions_json JSONB NOT NULL,stress_tests_json JSONB NOT NULL,data_timestamp TIMESTAMPTZ NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `CREATE INDEX IF NOT EXISTS idx_long_term_reviews_account ON long_term_portfolio_reviews(account_id,created_at DESC)`,
    ],
  },  {
    id: "0038_kids_monthly_contribution_reminders",
    description: "Durable monthly child and Roth contribution schedules, completion tracking, and reminders",
    statements: [
      `CREATE TABLE IF NOT EXISTS child_contribution_schedules (
        id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id) ON DELETE CASCADE,
        child_id TEXT NOT NULL REFERENCES child_profiles(id) ON DELETE CASCADE,
        goal_id TEXT NOT NULL REFERENCES child_goals(id) ON DELETE CASCADE,
        account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
        schedule_type TEXT NOT NULL CHECK(schedule_type IN ('KID_ACCOUNT','ROTH_IRA')),
        amount_cents BIGINT NOT NULL CHECK(amount_cents > 0), day_of_month INTEGER NOT NULL DEFAULT 1 CHECK(day_of_month BETWEEN 1 AND 28),
        active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE(goal_id,account_id)
      )`,
      `CREATE UNIQUE INDEX IF NOT EXISTS idx_child_monthly_expected ON child_contributions(goal_id,account_id,contribution_date) WHERE source='TRANSFER'`,
      `CREATE INDEX IF NOT EXISTS idx_child_contribution_schedules_household ON child_contribution_schedules(household_id,active,day_of_month)`,
    ],
  },
  {
    id: "0039_trade_lifecycle",
    description: "Durable positions, execution-linked exit episodes, lifecycle audit and hold counterfactuals",
    statements: [
      `CREATE TABLE IF NOT EXISTS position_states (account_id TEXT NOT NULL REFERENCES accounts(id), security_id TEXT NOT NULL REFERENCES securities(id), household_id TEXT NOT NULL REFERENCES households(id), state_json JSONB NOT NULL, action_json JSONB NOT NULL DEFAULT '{}', last_analysis_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(account_id,security_id))`,
      `CREATE TABLE IF NOT EXISTS trade_lifecycle_exits (id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id), account_id TEXT NOT NULL REFERENCES accounts(id), security_id TEXT NOT NULL REFERENCES securities(id), exit_transaction_id TEXT NOT NULL UNIQUE REFERENCES investment_transactions(id), recommendation_id TEXT REFERENCES recommendations(id), sell_reason TEXT, status TEXT NOT NULL, exit_json JSONB NOT NULL, plan_json JSONB, allocation_json JSONB NOT NULL DEFAULT '{}', outcome_json JSONB NOT NULL DEFAULT '{}', rebuy_json JSONB, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, last_analysis_at TIMESTAMPTZ)`,
      `CREATE INDEX IF NOT EXISTS idx_lifecycle_exits_monitor ON trade_lifecycle_exits(account_id,status,last_analysis_at)`,
      `ALTER TABLE trade_lifecycle_exits ADD COLUMN IF NOT EXISTS rotation_json JSONB`,
      `CREATE TABLE IF NOT EXISTS trade_lifecycle_events (id TEXT PRIMARY KEY, account_id TEXT NOT NULL REFERENCES accounts(id), security_id TEXT NOT NULL REFERENCES securities(id), event_key TEXT NOT NULL UNIQUE, event_type TEXT NOT NULL, snapshot_json JSONB NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `CREATE TABLE IF NOT EXISTS trade_lifecycle_reviews (id TEXT PRIMARY KEY, household_id TEXT NOT NULL REFERENCES households(id), account_id TEXT NOT NULL REFERENCES accounts(id), strategy_version TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'PENDING_REVIEW', evidence_json JSONB NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS sell_reason TEXT`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS lifecycle_audit_json JSONB NOT NULL DEFAULT '{}'`,
      `ALTER TABLE recommendations ADD COLUMN IF NOT EXISTS pipeline_status TEXT NOT NULL DEFAULT 'INCOMPLETE'`,
      `UPDATE recommendations SET actionable=FALSE WHERE action IN ('SELL','EXIT','REDUCE','TAKE_PARTIAL_PROFIT','THESIS_BROKEN') AND lifecycle_audit_json='{}'::jsonb`,
      `CREATE OR REPLACE FUNCTION guard_sell_lifecycle() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.action IN ('SELL','EXIT','REDUCE','TAKE_PARTIAL_PROFIT','THESIS_BROKEN') THEN IF NEW.sell_reason IS NULL OR NEW.sell_reason NOT IN ('THESIS BROKEN','TECHNICAL EXIT','STOP/INVALIDATION','TARGET REACHED','OVERVALUED/TRIM','CONCENTRATION REDUCTION','CAPITAL ROTATION','TACTICAL SELL FOR EXPECTED PULLBACK','GOAL/REBALANCE') OR (NEW.sell_reason NOT IN ('THESIS BROKEN','GOAL/REBALANCE','CAPITAL ROTATION') AND COALESCE(NEW.strategy_type,'') <> 'OPTIONS' AND (NEW.lifecycle_audit_json->'reentry' IS NULL OR NEW.lifecycle_audit_json->'reentry'='null'::jsonb)) OR COALESCE(NEW.lifecycle_audit_json->>'pipeline','') <> 'COMPLETE' OR NOT (NEW.lifecycle_audit_json ? 'allocation') THEN NEW.actionable=FALSE; NEW.pipeline_status='INCOMPLETE'; ELSE NEW.pipeline_status='COMPLETE'; END IF; END IF; RETURN NEW; END $$`,
      `DROP TRIGGER IF EXISTS recommendations_lifecycle_guard ON recommendations`,
      `CREATE TRIGGER recommendations_lifecycle_guard BEFORE INSERT OR UPDATE ON recommendations FOR EACH ROW EXECUTE FUNCTION guard_sell_lifecycle()`,
    ],
  },
  {
    id:"0040_central_ai_provider",
    description:"Strict snapshot-bound AI reasoning, approval controls, health and canonical account decisions",
    statements:[
      `CREATE TABLE IF NOT EXISTS ai_strategy_versions(version TEXT PRIMARY KEY,status TEXT NOT NULL CHECK(status IN ('DRAFT','APPROVED','RETIRED')),policy_json JSONB NOT NULL,approved_by TEXT,approved_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `INSERT INTO ai_strategy_versions(version,status,policy_json) VALUES('lifecycle-1.0.0','DRAFT','{"schemaVersion":"decision-reasoning-1","executionAllowed":false}') ON CONFLICT(version) DO NOTHING`,
      `ALTER TABLE ai_model_versions ADD COLUMN IF NOT EXISTS provider_model TEXT`,
      `ALTER TABLE ai_decision_runs ADD COLUMN IF NOT EXISTS snapshot_id TEXT`,
      `ALTER TABLE ai_decision_runs ADD COLUMN IF NOT EXISTS schema_version TEXT`,
      `ALTER TABLE ai_decision_runs ADD COLUMN IF NOT EXISTS provider_request_id TEXT`,
      `ALTER TABLE ai_decision_runs ADD COLUMN IF NOT EXISTS error_code TEXT`,
      `CREATE INDEX IF NOT EXISTS idx_ai_snapshot_reuse ON ai_decision_runs(account_id,ticker,snapshot_id,model_version,strategy_version,created_at DESC)`,
      `CREATE TABLE IF NOT EXISTS ai_current_decisions(account_id TEXT NOT NULL REFERENCES accounts(id),ticker TEXT NOT NULL,strategy TEXT NOT NULL,decision_id TEXT NOT NULL REFERENCES ai_decision_runs(id),snapshot_id TEXT NOT NULL,captured_at TIMESTAMPTZ NOT NULL,expires_at TIMESTAMPTZ NOT NULL,PRIMARY KEY(account_id,ticker,strategy))`,
      `CREATE TABLE IF NOT EXISTS ai_provider_health(provider TEXT PRIMARY KEY,last_success_at TIMESTAMPTZ,last_failure_at TIMESTAMPTZ,last_latency_ms INTEGER,last_model TEXT,last_model_version TEXT,successes BIGINT NOT NULL DEFAULT 0,failures BIGINT NOT NULL DEFAULT 0,fallback_state TEXT NOT NULL,error_code TEXT,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `CREATE TABLE IF NOT EXISTS ai_decision_requests(id TEXT PRIMARY KEY,decision_id TEXT NOT NULL UNIQUE REFERENCES ai_decision_runs(id),snapshot_id TEXT NOT NULL,schema_version TEXT NOT NULL,request_json JSONB NOT NULL,response_json JSONB,status TEXT NOT NULL,error_code TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
      `CREATE OR REPLACE FUNCTION guard_ai_recommendation() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.actionable AND NOT EXISTS(SELECT 1 FROM ai_decision_runs d JOIN ai_current_decisions c ON c.decision_id=d.id WHERE d.id=NEW.checks_json->'aiEvidence'->>'decisionId' AND d.ticker=(SELECT ticker FROM securities WHERE id=NEW.security_id) AND d.account_id=NEW.account_id AND d.household_id=NEW.household_id AND d.status='COMPLETED' AND d.output_json->>'providerStatus'='AVAILABLE' AND c.expires_at>CURRENT_TIMESTAMP) THEN NEW.actionable=FALSE; NEW.pipeline_status='INCOMPLETE'; END IF; RETURN NEW; END $$`,
      `DROP TRIGGER IF EXISTS recommendations_ai_guard ON recommendations`,
      `CREATE TRIGGER recommendations_ai_guard BEFORE INSERT OR UPDATE ON recommendations FOR EACH ROW EXECUTE FUNCTION guard_ai_recommendation()`,
      `UPDATE recommendations SET actionable=FALSE,pipeline_status='INCOMPLETE' WHERE actionable=TRUE AND checks_json->'aiEvidence' IS NULL`,
    ],
  },
  {id:"0041_entry_and_notification_integrity",description:"Confirmed entry gates and durable notification event timelines",statements:[
    `CREATE OR REPLACE FUNCTION guard_entry_recommendation() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE p JSONB; BEGIN p=NEW.checks_json->'aiEvidence'->'entryPlan'; IF NEW.actionable AND NEW.action IN ('BUY','STRONG_BUY','BUY_PARTIAL','ACCUMULATE') AND (p IS NULL OR COALESCE(p->>'status','')<>'CONFIRMED' OR COALESCE(p->>'orderType','NONE')='NONE' OR p->>'orderPrice' IS NULL OR COALESCE(p->>'confirmedAt','')='' OR COALESCE(p->>'expiresAt','')='') THEN NEW.actionable=FALSE; NEW.pipeline_status='INCOMPLETE'; END IF; RETURN NEW; END $$`,
    `CREATE TRIGGER recommendations_entry_guard BEFORE INSERT OR UPDATE ON recommendations FOR EACH ROW EXECUTE FUNCTION guard_entry_recommendation()`,
    `UPDATE recommendations SET actionable=FALSE,pipeline_status='INCOMPLETE' WHERE actionable=TRUE AND action IN ('BUY','STRONG_BUY','BUY_PARTIAL','ACCUMULATE') AND checks_json->'aiEvidence'->'entryPlan' IS NULL`,
    `CREATE TABLE notification_events(id TEXT PRIMARY KEY,household_id TEXT NOT NULL REFERENCES households(id),event_key TEXT NOT NULL,category TEXT NOT NULL,severity TEXT NOT NULL CHECK(severity IN ('CRITICAL','HIGH','MEDIUM','LOW')),title TEXT NOT NULL,summary TEXT NOT NULL,snapshot_json JSONB NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,UNIQUE(household_id,event_key))`,
    `CREATE TABLE notification_event_updates(id TEXT PRIMARY KEY,event_id TEXT NOT NULL REFERENCES notification_events(id),alert_id TEXT NOT NULL UNIQUE REFERENCES alerts(id) ON DELETE CASCADE,snapshot_json JSONB NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE notification_event_states(event_id TEXT NOT NULL REFERENCES notification_events(id),user_id TEXT NOT NULL REFERENCES users(id),read_at TIMESTAMPTZ,dismissed_at TIMESTAMPTZ,PRIMARY KEY(event_id,user_id))`,
    `CREATE TABLE notification_category_preferences(household_id TEXT NOT NULL REFERENCES households(id),user_id TEXT NOT NULL REFERENCES users(id),category TEXT NOT NULL,in_app BOOLEAN NOT NULL DEFAULT TRUE,push BOOLEAN NOT NULL DEFAULT TRUE,email BOOLEAN NOT NULL DEFAULT FALSE,PRIMARY KEY(household_id,user_id,category))`,
    `CREATE TABLE notification_push_receipts(delivery_id TEXT NOT NULL REFERENCES alert_deliveries(id) ON DELETE CASCADE,subscription_id TEXT NOT NULL REFERENCES push_subscriptions(id),accepted_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(delivery_id,subscription_id))`,
    `CREATE INDEX idx_notification_events_household ON notification_events(household_id,updated_at DESC)`,
  ]},
  {id:"0042_configuration_and_strategy_positions",description:"Audited settings and explicit strategy ownership",statements:[
    `CREATE TABLE app_user_settings(household_id TEXT NOT NULL REFERENCES households(id),user_id TEXT NOT NULL REFERENCES users(id),settings_json JSONB NOT NULL,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(household_id,user_id))`,
    `CREATE TABLE account_risk_config(account_id TEXT PRIMARY KEY REFERENCES accounts(id),recommended_json JSONB NOT NULL,custom_json JSONB,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE provider_health_checks(provider TEXT PRIMARY KEY,status TEXT NOT NULL,last_success_at TIMESTAMPTZ,last_error TEXT,checked_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE strategy_positions(id TEXT PRIMARY KEY,account_id TEXT NOT NULL REFERENCES accounts(id),security_id TEXT NOT NULL REFERENCES securities(id),strategy TEXT NOT NULL CHECK(strategy IN ('SWING','DAY_TRADE','LONG_TERM')),shares NUMERIC NOT NULL DEFAULT 0 CHECK(shares>=0),average_cost NUMERIC NOT NULL DEFAULT 0,stop NUMERIC,opened_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,closed_at TIMESTAMPTZ,realized_pnl NUMERIC NOT NULL DEFAULT 0)`,
    `CREATE UNIQUE INDEX idx_strategy_position_open ON strategy_positions(account_id,security_id,strategy) WHERE closed_at IS NULL`,
    `CREATE TABLE strategy_position_fills(transaction_id TEXT PRIMARY KEY REFERENCES investment_transactions(id),position_id TEXT NOT NULL REFERENCES strategy_positions(id),shares NUMERIC NOT NULL,price NUMERIC NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
  ]},
  {id:"0043_post_trade_reviews",description:"Immutable predictions and versioned observational post-trade reviews",statements:[
    `CREATE TABLE post_trade_reviews(exit_id TEXT PRIMARY KEY REFERENCES trade_lifecycle_exits(id),input_json JSONB NOT NULL,review_json JSONB NOT NULL,revision_hash TEXT NOT NULL,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE post_trade_review_revisions(exit_id TEXT NOT NULL REFERENCES trade_lifecycle_exits(id),revision_hash TEXT NOT NULL,review_json JSONB NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(exit_id,revision_hash))`,
  ]},
  {id:"0044_worker_run_lease",description:"Durable whole-worker concurrency control",statements:[
    `CREATE TABLE worker_run_leases(name TEXT PRIMARY KEY,owner TEXT NOT NULL,expires_at TIMESTAMPTZ NOT NULL)`,
  ]},
 {id:"0045_discovery_queue_rotation",description:"Persistent discovery coverage, provider budgets and pre-sale rotation reviews",statements:[
  `CREATE TABLE discovery_control(id TEXT PRIMARY KEY,refreshed_at TIMESTAMPTZ,cooldown_until TIMESTAMPTZ,last_error TEXT)`,
  `CREATE TABLE discovery_queue(symbol TEXT PRIMARY KEY,asset_json JSONB NOT NULL,active BOOLEAN NOT NULL DEFAULT TRUE,priority INTEGER NOT NULL DEFAULT 0,stage TEXT NOT NULL DEFAULT 'NOT_SCANNED',seed_json JSONB,last_screened_at TIMESTAMPTZ,last_researched_at TIMESTAMPTZ,next_screen_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,next_research_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,lease_until TIMESTAMPTZ,error_code TEXT,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
  `CREATE INDEX discovery_queue_screen ON discovery_queue(next_screen_at,priority) WHERE active`,
  `CREATE INDEX discovery_queue_research ON discovery_queue(next_research_at,priority) WHERE active AND seed_json IS NOT NULL`,
  `CREATE TABLE discovery_provider_cache(cache_key TEXT PRIMARY KEY,payload_json JSONB NOT NULL,fetched_at TIMESTAMPTZ NOT NULL,expires_at TIMESTAMPTZ NOT NULL)`,
  `CREATE TABLE rotation_reviews(id TEXT PRIMARY KEY,household_id TEXT NOT NULL REFERENCES households(id),account_id TEXT NOT NULL REFERENCES accounts(id),ticker TEXT NOT NULL,cycle_key TEXT NOT NULL,snapshot_json JSONB NOT NULL,outcome_json JSONB,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,UNIQUE(account_id,ticker,cycle_key))`,
  `CREATE INDEX rotation_reviews_account ON rotation_reviews(account_id,created_at DESC)`,
 ]}, {id:"0046_ai_event_job_type",description:"Allow central per-ticker AI review jobs in the durable queue",statements:[
 `ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
 `ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT','ACCOUNT_INTELLIGENCE_LOOP','KIDS_PLAN_REVIEW','AI_EVENT_REVIEW'))`,
 ]}, {id:"0047_active_analysis_queue_indexes",description:"Bound active-job deduplication and account decision lookup cost",statements:[
 `CREATE INDEX IF NOT EXISTS background_jobs_active_scope ON background_jobs(job_type,household_id,(payload_json->>'accountId'),(payload_json->>'symbol')) WHERE status IN ('QUEUED','RUNNING','FAILED')`,
 `CREATE INDEX IF NOT EXISTS recommendations_account_security_latest ON recommendations(account_id,security_id,created_at DESC)`,
 `CREATE INDEX IF NOT EXISTS rotation_reviews_pending_outcomes ON rotation_reviews(account_id,created_at) WHERE outcome_json IS NULL AND snapshot_json->>'status'='REVIEW_READY'`,
 ]}, {id:"0048_quant_data_evidence",description:"Optional cached market structure evidence and monitoring jobs",statements:[
`CREATE TABLE IF NOT EXISTS quant_data_prints (provider_id TEXT PRIMARY KEY,symbol TEXT NOT NULL,event_at TIMESTAMPTZ NOT NULL,payload JSONB NOT NULL,captured_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`,
`CREATE INDEX IF NOT EXISTS quant_data_prints_symbol_time ON quant_data_prints(symbol,event_at)`,
`CREATE TABLE IF NOT EXISTS quant_data_cache (cache_key TEXT PRIMARY KEY, payload JSONB, expires_at TIMESTAMPTZ, lease_until TIMESTAMPTZ, updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`,
`ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
`ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT','ACCOUNT_INTELLIGENCE_LOOP','KIDS_PLAN_REVIEW','AI_EVENT_REVIEW','QUANT_FLOW'))`,
]}, {id:"0049_options_discovery",description:"Broad-universe option screening and account decision snapshots",statements:[
`CREATE TABLE options_discovery(symbol TEXT PRIMARY KEY REFERENCES discovery_queue(symbol),stage TEXT NOT NULL DEFAULT 'NOT_SCANNED',reason TEXT NOT NULL DEFAULT 'Awaiting broad price/liquidity screen',payload_json JSONB NOT NULL DEFAULT '{}',checked_at TIMESTAMPTZ,next_check_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,lease_until TIMESTAMPTZ)`,
`CREATE INDEX options_discovery_due ON options_discovery(next_check_at,checked_at)`,
`CREATE TABLE options_account_decisions(account_id TEXT NOT NULL REFERENCES accounts(id),symbol TEXT NOT NULL,decision_json JSONB NOT NULL,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(account_id,symbol))`,
`CREATE TABLE options_account_history(id TEXT PRIMARY KEY,account_id TEXT NOT NULL REFERENCES accounts(id),symbol TEXT NOT NULL,decision_json JSONB NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
`ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
`ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT','ACCOUNT_INTELLIGENCE_LOOP','KIDS_PLAN_REVIEW','AI_EVENT_REVIEW','QUANT_FLOW','OPTIONS_DISCOVERY','OPTIONS_ACCOUNT_REVIEW'))`,
]}, {id:"0050_discovery_missing_evidence",description:"Preserve unavailable discovery scores and revalidate older research",statements:[
`ALTER TABLE market_discovery_candidates ALTER COLUMN business_quality DROP NOT NULL,ALTER COLUMN growth_acceleration DROP NOT NULL,ALTER COLUMN valuation DROP NOT NULL`,
`UPDATE market_discovery_candidates SET business_quality=NULL,growth_acceleration=NULL,valuation=NULL,scores_json='{}'::jsonb,discovery_confidence=0,status='EARLY_WATCH',why_found='Prior research requires evidence-score revalidation',model_version='AWAITING_EVIDENCE_REVALIDATION' WHERE model_version<>'discovery-v5-evidence'`,
`UPDATE discovery_queue SET stage='RESEARCH_PENDING',next_research_at=CURRENT_TIMESTAMP WHERE active AND seed_json IS NOT NULL AND last_researched_at IS NOT NULL`,
]}, {id:"0051_account_strategies",description:"Separate investment strategies from account goals and permit ETF-only portfolios",statements:[
`ALTER TABLE investment_account_settings DROP CONSTRAINT IF EXISTS investment_account_settings_strategy_type_check`,
`ALTER TABLE investment_account_settings ADD CONSTRAINT investment_account_settings_strategy_type_check CHECK(strategy_type IN ('SWING','OPTIONS','LONG_TERM','LONG_TERM_ETF','GROWTH_5_7','RETIREMENT','CHILD_GROWTH','COLLEGE','AGGRESSIVE_GROWTH','DIVIDEND_INCOME','HOUSE_FUND','CAPITAL_PRESERVATION','CUSTOM'))`,
`INSERT INTO account_allocation_targets(id,account_id,category,target_bps)
 SELECT 'goal_migration:'||s.account_id||':'||t.key,s.account_id,t.key,t.value::integer FROM investment_account_settings s,
 jsonb_each_text(CASE s.strategy_type
 WHEN 'RETIREMENT' THEN '{"CORE":5500,"QUALITY":1500,"AGGRESSIVE":500,"INCOME":1500,"BONDS":700,"CASH":300}'::jsonb
 WHEN 'CHILD_GROWTH' THEN '{"CORE":6500,"QUALITY":2000,"AGGRESSIVE":750,"INCOME":250,"BONDS":250,"CASH":250}'::jsonb
 WHEN 'COLLEGE' THEN '{"CORE":5000,"QUALITY":1000,"AGGRESSIVE":250,"INCOME":500,"BONDS":2500,"CASH":750}'::jsonb
 WHEN 'HOUSE_FUND' THEN '{"CORE":2000,"QUALITY":500,"AGGRESSIVE":0,"INCOME":500,"BONDS":3500,"CASH":3500}'::jsonb
 ELSE '{}'::jsonb END) t
 WHERE NOT EXISTS (SELECT 1 FROM account_allocation_targets a WHERE a.account_id=s.account_id)
 ON CONFLICT(account_id,category) DO NOTHING`,
`UPDATE investment_account_settings SET policy_json=COALESCE(policy_json,'{}'::jsonb)||jsonb_build_object('legacyStrategy',strategy_type),
 goal_name=CASE WHEN goal_name IN ('Build wealth','Retirement','Child Growth','College','House Fund','CHILD_GROWTH','COLLEGE','HOUSE_FUND','RETIREMENT') THEN CASE strategy_type WHEN 'RETIREMENT' THEN 'Retirement' WHEN 'CHILD_GROWTH' THEN 'Future Wealth' WHEN 'COLLEGE' THEN 'Education' WHEN 'HOUSE_FUND' THEN 'House Purchase' ELSE goal_name END ELSE goal_name END,
 strategy_type='LONG_TERM' WHERE strategy_type IN ('GROWTH_5_7','RETIREMENT','CHILD_GROWTH','COLLEGE','HOUSE_FUND')`,
`UPDATE accounts SET investment_purpose='Long-term' WHERE investment_purpose='Retirement'`,
]}, {id:"0052_transaction_notification_audit",description:"Persist per-recipient transaction notification decisions",statements:[
`CREATE TABLE transaction_notification_audit(event_id TEXT NOT NULL REFERENCES transaction_notification_events(id) ON DELETE CASCADE,user_id TEXT NOT NULL REFERENCES users(id),household_id TEXT NOT NULL REFERENCES households(id),reason TEXT NOT NULL,channels_json JSONB NOT NULL DEFAULT '[]',available_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(event_id,user_id))`,
`CREATE INDEX transaction_notification_audit_household ON transaction_notification_audit(household_id,created_at DESC)`,
]}, {id:"0053_options_research_pipeline",description:"Durable option research stages and deduplicated refresh requests",statements:[
`CREATE TABLE options_research_stages(account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,symbol TEXT NOT NULL,research_version TEXT NOT NULL,stage TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'QUEUED',job_id TEXT,started_at TIMESTAMPTZ,completed_at TIMESTAMPTZ,retry_count INTEGER NOT NULL DEFAULT 0,last_error TEXT,freshness TEXT NOT NULL DEFAULT 'QUEUED',next_retry_at TIMESTAMPTZ,details_json JSONB NOT NULL DEFAULT '{}',updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(account_id,symbol,research_version,stage))`,
`CREATE INDEX options_research_stage_account ON options_research_stages(account_id,status,updated_at DESC)`,
`WITH ranked AS (SELECT id,row_number() OVER(PARTITION BY payload_json->>'accountId',payload_json->>'symbol' ORDER BY CASE status WHEN 'RUNNING' THEN 0 ELSE 1 END,created_at) n FROM background_jobs WHERE job_type='OPTIONS_ACCOUNT_REVIEW' AND status IN ('QUEUED','RUNNING','FAILED') AND attempts<6) UPDATE background_jobs j SET status='DEAD',error_code='DUPLICATE_RESEARCH_JOB',updated_at=CURRENT_TIMESTAMP FROM ranked r WHERE r.id=j.id AND r.n>1`,
`CREATE UNIQUE INDEX options_active_research_job ON background_jobs((payload_json->>'accountId'),(payload_json->>'symbol')) WHERE job_type='OPTIONS_ACCOUNT_REVIEW' AND status IN ('QUEUED','RUNNING','FAILED') AND attempts<6`,
]}, {id:"0054_account_market_search",description:"Account-wide market search and ranked alternative evidence",statements:[
`CREATE TABLE account_search_runs(id TEXT PRIMARY KEY,household_id TEXT NOT NULL REFERENCES households(id),account_id TEXT NOT NULL REFERENCES accounts(id),status TEXT NOT NULL,stage TEXT NOT NULL,counts_json JSONB NOT NULL DEFAULT '{}',last_error TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,completed_at TIMESTAMPTZ)`,
`CREATE INDEX account_search_latest ON account_search_runs(account_id,created_at DESC)`,
`CREATE TABLE account_search_candidates(run_id TEXT NOT NULL REFERENCES account_search_runs(id) ON DELETE CASCADE,symbol TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'QUEUED',rank_score NUMERIC NOT NULL DEFAULT 0,decision_json JSONB NOT NULL DEFAULT '{}',last_error TEXT,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(run_id,symbol))`,
`ALTER TABLE background_jobs DROP CONSTRAINT IF EXISTS background_jobs_job_type_check`,
`ALTER TABLE background_jobs ADD CONSTRAINT background_jobs_job_type_check CHECK(job_type IN ('PLAID_SYNC','PLAID_INVESTMENT_SYNC','NOTIFICATION_DELIVERY','MARKET_INTELLIGENCE','MARKET_DISCOVERY','DAILY_CLOSE_REVIEW','OVERNIGHT_OUTLOOK_REFRESH','TACTICAL_REENTRY_MONITOR','OPTIONS_FLOW','INVESTMENT_COVERAGE_AUDIT','ACCOUNT_INTELLIGENCE_LOOP','KIDS_PLAN_REVIEW','AI_EVENT_REVIEW','QUANT_FLOW','OPTIONS_DISCOVERY','OPTIONS_ACCOUNT_REVIEW','ACCOUNT_OPPORTUNITY_REVIEW'))`,
]},] as const;
