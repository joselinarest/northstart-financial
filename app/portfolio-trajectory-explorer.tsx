"use client";
import {useState} from "react";
type Point={year:number;low:number;base:number;high:number};
const money=(value:number)=>`$${value.toLocaleString(undefined,{maximumFractionDigits:0})}`;
export default function PortfolioTrajectoryExplorer({points,horizon}:{points:Point[];horizon:string}){const[selected,setSelected]=useState(points.length-1),all=points.flatMap(point=>[point.low,point.base,point.high]),min=Math.min(...all),max=Math.max(...all),range=Math.max(1,max-min),x=(index:number)=>index/Math.max(1,points.length-1)*100,y=(value:number)=>92-(value-min)/range*82,path=(key:"low"|"base"|"high")=>points.map((point,index)=>`${x(index)},${y(point[key])}`).join(" "),active=points[selected]||points.at(-1)!;return <details className="interactive-trajectory" open><summary><span>MODELED TRAJECTORY · {horizon.toUpperCase()}</span><b>Explore how the target could grow—or decline</b><i>Show / hide</i></summary><div className="interactive-trajectory-chart"><svg viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label="Interactive stress, planning and stronger portfolio scenarios"><polyline className="low" points={path("low")}/><polyline className="base" points={path("base")}/><polyline className="high" points={path("high")}/>{points.map((point,index)=><g key={point.year} className={selected===index?"selected":""} onMouseEnter={()=>setSelected(index)} onClick={()=>setSelected(index)} tabIndex={0} onFocus={()=>setSelected(index)} role="button" aria-label={`Year ${point.year}: stress ${money(point.low)}, planning ${money(point.base)}, stronger ${money(point.high)}`}><line x1={x(index)} x2={x(index)} y1="5" y2="95"/><circle className="low" cx={x(index)} cy={y(point.low)} r="1.6"/><circle className="base" cx={x(index)} cy={y(point.base)} r="1.8"/><circle className="high" cx={x(index)} cy={y(point.high)} r="1.6"/></g>)}</svg><div className="trajectory-year-buttons">{points.map((point,index)=><button className={selected===index?"active":""} key={point.year} onMouseEnter={()=>setSelected(index)} onClick={()=>setSelected(index)}>Year {point.year}</button>)}</div></div><div className="interactive-trajectory-values"><b>Year {active.year}</b><span className="low">Stress <strong>{money(active.low)}</strong></span><span className="base">Planning <strong>{money(active.base)}</strong></span><span className="high">Stronger <strong>{money(active.high)}</strong></span><small>Modeled scenarios are uncertain and are not promised returns.</small></div></details>}
