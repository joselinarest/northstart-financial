import{workspace,id}from"@/lib/db";import{encryptSecret}from"@/lib/crypto";import{loadRuntimeSecrets}from"@/lib/runtime-secrets";
const hash=async(value:string)=>Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value))),byte=>byte.toString(16).padStart(2,"0")).join("");
export async function GET(request:Request){try{await workspace(request);await loadRuntimeSecrets();return Response.json({publicKey:process.env.VAPID_PUBLIC_KEY||null,configured:Boolean(process.env.VAPID_PUBLIC_KEY&&process.env.PUSH_DELIVERY_URL)})}catch(error){if(error instanceof Response)return error;return Response.json({error:"Push configuration unavailable"},{status:500})}}
export async function POST(request:Request){try{const{db,householdId,userId}=await workspace(request),subscription=await request.json() as{endpoint?:string};if(!subscription.endpoint)return Response.json({error:"A valid push subscription is required"},{status:400});const endpointHash=await hash(subscription.endpoint),encrypted=await encryptSecret(JSON.stringify(subscription));await db.prepare("INSERT INTO push_subscriptions(id,household_id,user_id,endpoint_hash,encrypted_subscription,active,updated_at) VALUES(?,?,?,?,?,TRUE,CURRENT_TIMESTAMP) ON CONFLICT(endpoint_hash) DO UPDATE SET household_id=excluded.household_id,user_id=excluded.user_id,encrypted_subscription=excluded.encrypted_subscription,active=TRUE,updated_at=CURRENT_TIMESTAMP").bind(id("push_subscription"),householdId,userId,endpointHash,encrypted).run();return Response.json({ok:true})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Push subscription could not be saved"},{status:400})}}
export async function DELETE(request:Request){try{const{db,householdId,userId}=await workspace(request);await db.prepare("UPDATE push_subscriptions SET active=FALSE,updated_at=CURRENT_TIMESTAMP WHERE household_id=? AND user_id=?").bind(householdId,userId).run();return Response.json({ok:true})}catch(error){if(error instanceof Response)return error;return Response.json({error:"Push subscription could not be disabled"},{status:400})}}
