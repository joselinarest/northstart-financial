import{workspace}from"@/lib/db";
export const dynamic="force-dynamic";
export async function GET(request:Request){try{const{db,householdId,userId}=await workspace(request),url=new URL(request.url),limit=Math.min(200,Math.max(1,Number(url.searchParams.get("limit")||100))),result=await db.prepare(`SELECT e.id event_id,e.event_type,e.severity,e.snapshot_json,e.created_at,a.id alert_id,a.title,a.explanation,a.read_at,d.channel,d.status delivery_status,d.attempted_at,d.delivered_at,d.error_code FROM transaction_notification_events e JOIN alerts a ON a.id=('alert_'||e.id) LEFT JOIN alert_deliveries d ON d.alert_id=a.id AND d.user_id=? WHERE e.household_id=? ORDER BY e.created_at DESC,d.channel LIMIT ?`).bind(userId,householdId,limit).all();return Response.json({notifications:result.results},{headers:{"Cache-Control":"private, no-store"}})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Notification history unavailable"},{status:500})}}
