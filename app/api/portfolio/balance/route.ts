import{workspace}from"@/lib/db";import{evaluateLongTermPortfolio}from"@/lib/long-term-balance-engine";export const dynamic="force-dynamic";export async function GET(r:Request){try{const{db,householdId}=await workspace(r),accountId=new URL(r.url).searchParams.get("accountId");if(!accountId)return Response.json({error:"accountId is required"},{status:400});return Response.json(await evaluateLongTermPortfolio(db,householdId,accountId),{headers:{"Cache-Control":"private, no-store"}})}catch(e){if(e instanceof Response)return e;return Response.json({error:e instanceof Error?e.message:"Portfolio balance unavailable"},{status:500})}}
