import { workspace } from "@/lib/db";
import { calculatePortfolio, strategyTarget, type AllocationCategory } from "@/lib/domain/portfolio";
import type { StrategyType } from "@/lib/domain/investment-accounts";

export const dynamic="force-dynamic";

export async function GET(request:Request){try{const{db,householdId}=await workspace(request),accountId=new URL(request.url).searchParams.get("accountId");if(!accountId)return Response.json({error:"accountId is required"},{status:400});const account=await db.prepare(`SELECT a.id,a.nickname,a.name,a.investment_purpose,COALESCE(s.strategy_type,'GROWTH_5_7') strategy_type,COALESCE(s.share_mode,'WHOLE') share_mode,COALESCE(s.maximum_position_bps,1000) maximum_position_bps,COALESCE(s.available_cash_cents,0) available_cash_cents,s.horizon_months,s.policy_json FROM accounts a JOIN entities e ON e.id=a.entity_id LEFT JOIN investment_account_settings s ON s.account_id=a.id WHERE a.id=? AND e.household_id=? AND a.type='investment'`).bind(accountId,householdId).first<Record<string,any>>();if(!account)return Response.json({error:"Investment account not found"},{status:404});const[holdingResult,targetResult,liabilityResult,spendingResult,liquidResult]=await Promise.all([db.prepare(`SELECT h.id,s.ticker symbol,s.name,s.type security_type,h.quantity::text,h.price_cents,COALESCE(h.quantity*h.price_cents,0)::bigint market_value_cents,h.cost_basis_cents FROM holdings h JOIN securities s ON s.id=h.security_id WHERE h.account_id=?`).bind(accountId).all<Record<string,any>>(),db.prepare("SELECT category,target_bps FROM account_allocation_targets WHERE account_id=?").bind(accountId).all<Record<string,any>>(),db.prepare("SELECT COALESCE(SUM(current_balance_cents),0)::bigint high_interest_debt_cents FROM liabilities l JOIN entities e ON e.id=l.entity_id WHERE e.household_id=? AND COALESCE(l.apr_bps,0)>=800").bind(householdId).first<Record<string,any>>(),db.prepare("SELECT COALESCE(SUM(amount_cents),0)::bigint spending_cents FROM transactions t JOIN entities e ON e.id=t.entity_id WHERE e.household_id=? AND t.direction='outflow' AND t.excluded=0 AND t.posted_at>=CURRENT_TIMESTAMP-INTERVAL '90 days'").bind(householdId).first<Record<string,any>>(),db.prepare("SELECT COALESCE(SUM(GREATEST(a.current_balance_cents,0)),0)::bigint liquid_cents FROM accounts a JOIN entities e ON e.id=a.entity_id WHERE e.household_id=? AND (a.type IN ('depository','cash') OR a.subtype IN ('checking','savings','money market'))").bind(householdId).first<Record<string,any>>()]);const custom=Object.fromEntries(targetResult.results.map(row=>[String(row.category),Number(row.target_bps)])) as Partial<Record<AllocationCategory,number>>,target=strategyTarget(String(account.strategy_type) as StrategyType,Object.keys(custom).length?custom:undefined),highInterestDebt=BigInt(String(liabilityResult?.high_interest_debt_cents||0)),quarterSpending=BigInt(String(spendingResult?.spending_cents||0)),monthlySpending=quarterSpending/3n,emergencyTarget=monthlySpending*6n,liquid=BigInt(String(liquidResult?.liquid_cents||0)),reserveGap=emergencyTarget>liquid?emergencyTarget-liquid:0n,accountCash=BigInt(String(account.available_cash_cents||0)),safeCapacity=highInterestDebt>0n||reserveGap>0n?0n:accountCash,policy=typeof account.policy_json==="object"&&account.policy_json?account.policy_json:{},goalTarget=policy.goalTargetCents?BigInt(String(policy.goalTargetCents)):null,annualContribution=policy.annualContributionCents?BigInt(String(policy.annualContributionCents)):0n,portfolio=calculatePortfolio({holdings:holdingResult.results.map(row=>({id:String(row.id),symbol:String(row.symbol||""),name:String(row.name||row.symbol||"Security"),securityType:row.security_type?String(row.security_type):null,quantity:String(row.quantity||"0"),marketValueCents:BigInt(String(row.market_value_cents||0)),priceCents:BigInt(String(row.price_cents||0)),costBasisCents:row.cost_basis_cents==null?null:BigInt(String(row.cost_basis_cents))})),cashCents:accountCash,target,shareMode:account.share_mode,maximumPositionBps:Number(account.maximum_position_bps),safeInvestmentCapacityCents:safeCapacity,goalTargetCents:goalTarget,horizonMonths:Number(account.horizon_months||60),annualContributionCents:annualContribution});return Response.json({account:{id:account.id,name:account.nickname||account.name,strategyType:account.strategy_type,shareMode:account.share_mode,horizonMonths:account.horizon_months},householdSafety:{highInterestDebtCents:highInterestDebt.toString(),monthlySpendingCents:monthlySpending.toString(),emergencyTargetCents:emergencyTarget.toString(),liquidCents:liquid.toString(),reserveGapCents:reserveGap.toString(),safeInvestmentCapacityCents:safeCapacity.toString(),status:highInterestDebt>0n?"BLOCKED_HIGH_INTEREST_DEBT":reserveGap>0n?"BLOCKED_EMERGENCY_RESERVE":"READY"},target,portfolio},{headers:{"Cache-Control":"private, no-store"}})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Portfolio advice unavailable"},{status:500})}}

