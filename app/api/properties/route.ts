import{workspace,id}from"@/lib/db";export const dynamic="force-dynamic";
export async function GET(r:Request){try{const{db,householdId}=await workspace(r);return Response.json((await db.prepare("SELECT p.*,e.name entity_name FROM properties p JOIN entities e ON e.id=p.entity_id WHERE e.household_id=?").bind(householdId).all()).results)}catch(e){if(e instanceof Response)return e;throw e}}
export async function POST(r:Request){try{const{db,householdId}=await workspace(r),b=await r.json()as Record<string,unknown>;if(!b.name)return Response.json({error:"Property name required"},{status:400});const eid=id("entity"),pid=id("property");await db.batch([db.prepare("INSERT INTO entities(id,household_id,type,name,legal_name) VALUES(?,?,?,?,?)").bind(eid,householdId,"property",b.name,b.legalName||null),db.prepare("INSERT INTO properties(id,entity_id,address,property_type,purchase_price_cents,estimated_value_cents,monthly_rent_cents,monthly_expenses_cents) VALUES(?,?,?,?,?,?,?,?)").bind(pid,eid,b.address||null,b.propertyType||"residential",b.purchasePriceCents||null,b.estimatedValueCents||null,b.monthlyRentCents||0,b.monthlyExpensesCents||0)]);return Response.json({id:pid,entityId:eid},{status:201})}catch(e){if(e instanceof Response)return e;throw e}}
