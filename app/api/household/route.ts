import{workspace}from"@/lib/db";export const dynamic="force-dynamic";
export async function GET(r:Request){try{const{db,householdId,role,userId}=await workspace(r);const [household,members,invitations,available]=await Promise.all([db.prepare("SELECT id,name,base_currency,timezone FROM households WHERE id=?").bind(householdId).first(),db.prepare("SELECT hm.user_id,hm.role,hm.status,u.email,u.display_name FROM household_members hm JOIN users u ON u.id=hm.user_id WHERE hm.household_id=? ORDER BY CASE hm.role WHEN 'owner' THEN 0 ELSE 1 END,u.display_name").bind(householdId).all(),role==="owner"?db.prepare("SELECT id,email,role,status,expires_at,created_at FROM household_invitations WHERE household_id=? AND status='pending' ORDER BY created_at DESC").bind(householdId).all():Promise.resolve({results:[]}),db.prepare("SELECT h.id,h.name,hm.role FROM household_members hm JOIN households h ON h.id=hm.household_id WHERE hm.user_id=? AND hm.status='active' ORDER BY h.name").bind(userId).all()]);return Response.json({household,role,members:members.results,invitations:invitations.results,availableHouseholds:available.results})}catch(e){if(e instanceof Response)return e;throw e}}
