import {id} from "@/lib/db";
import {workspace} from "@/lib/db";
import {listMarketDiscoveries} from "@/lib/market-discovery-engine";
export const dynamic="force-dynamic";export const runtime="nodejs";
export async function GET(request:Request){try{const{db}=await workspace(request),url=new URL(request.url),result=await listMarketDiscoveries(db,url.searchParams.get("search")||"",url.searchParams.get("strategy")||"ALL"),rows=result.candidates as Record<string,any>[],minimumConfidence=62,nearMisses=rows.filter(row=>row.status==="REJECTED").sort((a,b)=>Number(b.discovery_confidence)-Number(a.discovery_confidence)).slice(0,5);return Response.json({...result,scan:{status:result.run?.status||"NEVER_RUN",lastSuccessfulScan:result.lastSuccessfulScan,universeSize:Number(result.run?.universe_size||0),companiesEvaluated:Number(result.run?.screened_count||0),candidatesAccepted:Number(result.run?.candidates_created||0),candidatesRejected:Number(result.run?.rejected_count||0),minimumConfidence,providerErrors:result.run?.provider_errors_json||[],aiAnalysisFailures:Number(result.run?.ai_analysis_failures||0),errorCode:result.run?.error_code||null},nearMisses},{headers:{"Cache-Control":"private, no-store"}})}catch(error){if(error instanceof Response)return error;console.error("candidate.frontend_api_fetch_failed",{message:error instanceof Error?error.message:String(error)});return Response.json({error:"Candidate discovery API unavailable",detail:error instanceof Error?error.message:String(error)},{status:500})}}
export async function POST(request:Request){try{const{db}=await workspace(request),bucket=new Date().toISOString().slice(0,16),job=await db.prepare("INSERT INTO background_jobs(id,job_type,idempotency_key,payload_json) VALUES(?,'MARKET_DISCOVERY',?,?::jsonb) ON CONFLICT(idempotency_key) DO NOTHING RETURNING id").bind(id("job"),`manual-discovery:${bucket}`,JSON.stringify({requestedAt:new Date().toISOString(),source:"manual"})).first<{id:string}>();console.info("candidate.scan_queued",{jobId:job?.id||null,bucket,source:"manual"});return Response.json({queued:Boolean(job),jobId:job?.id||null,status:job?"QUEUED":"ALREADY_QUEUED"},{status:202})}catch(error){if(error instanceof Response)return error;console.error("candidate.scan_enqueue_failed",{message:error instanceof Error?error.message:String(error)});return Response.json({error:"Candidate scan could not be queued",detail:error instanceof Error?error.message:String(error)},{status:500})}}