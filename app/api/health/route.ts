import { database } from "@/lib/db";export const dynamic="force-dynamic";export async function GET(){try{const db=await database();const row=await db.prepare("SELECT 1 ok").first<{ok:number}>();return Response.json({ok:Number(row?.ok)===1,database:"AWS PostgreSQL",documents:"PostgreSQL BYTEA",connections:"read-only"})}catch(error){return Response.json({ok:false,database:"AWS PostgreSQL",error:error instanceof Error?error.message:"Database unavailable"},{status:503})}}
