import{workspace,id}from"@/lib/db";
import{evaluatePrediction,type PredictionInput,validatePrediction}from"@/lib/domain/predictions";
export const dynamic="force-dynamic";

async function securityFor(db:any,symbol:string){return db.prepare("SELECT id,ticker,name FROM securities WHERE ticker=? ORDER BY id LIMIT 1").bind(symbol).first<{id:string;ticker:string;name:string}>()}

export async function GET(request:Request){try{const{db,householdId}=await workspace(request),url=new URL(request.url),accountId=url.searchParams.get("accountId"),symbol=String(url.searchParams.get("symbol")||"").toUpperCase(),limit=Math.min(100,Math.max(1,Number(url.searchParams.get("limit")||25)));const clauses=["p.household_id=?"],values:any[]=[householdId];if(accountId){clauses.push("p.account_id=?");values.push(accountId)}if(symbol){clauses.push("s.ticker=?");values.push(symbol)}const result=await db.prepare(`SELECT p.id,p.account_id,s.ticker symbol,s.name,p.horizon,p.model_version,p.feature_fingerprint,p.input_snapshot_json,p.scenarios_json,p.confidence,p.evidence_as_of,p.expires_at,p.created_at,COALESCE(json_agg(json_build_object('scenario',pp.scenario,'pointAt',pp.point_at,'priceCents',pp.price_cents::text,'lowerCents',pp.lower_cents::text,'upperCents',pp.upper_cents::text) ORDER BY pp.point_at) FILTER (WHERE pp.id IS NOT NULL),'[]') points FROM predictions p JOIN securities s ON s.id=p.security_id LEFT JOIN prediction_points pp ON pp.prediction_id=p.id WHERE ${clauses.join(" AND ")} GROUP BY p.id,s.ticker,s.name ORDER BY p.created_at DESC LIMIT ?`).bind(...values,limit).all();return Response.json({predictions:result.results},{headers:{"Cache-Control":"private, no-store"}})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Predictions unavailable"},{status:500})}}

export async function POST(request:Request){try{const{db,householdId}=await workspace(request),input=validatePrediction(await request.json()as PredictionInput);if(input.accountId){const account=await db.prepare("SELECT a.id FROM accounts a JOIN entities e ON e.id=a.entity_id WHERE a.id=? AND e.household_id=? AND a.type='investment'").bind(input.accountId,householdId).first();if(!account)return Response.json({error:"Investment account not found"},{status:404})}let security=await securityFor(db,input.symbol);if(!security){const securityId=id("security");await db.prepare("INSERT INTO securities(id,ticker,name,type,currency) VALUES(?,?,?,'stock','USD') ON CONFLICT(ticker,type) DO NOTHING").bind(securityId,input.symbol,input.symbol).run();security=await securityFor(db,input.symbol)}if(!security)return Response.json({error:"Security could not be resolved"},{status:500});const predictionId=id("prediction");await db.transaction(async tx=>{await tx.prepare("INSERT INTO predictions(id,household_id,account_id,security_id,horizon,model_version,feature_fingerprint,input_snapshot_json,scenarios_json,confidence,evidence_as_of,expires_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)").bind(predictionId,householdId,input.accountId||null,security!.id,input.horizon,input.modelVersion,input.featureFingerprint,JSON.stringify(input.inputSnapshot),JSON.stringify(input.scenarios),input.confidence,input.evidenceAsOf,input.expiresAt).run();for(const point of input.points)await tx.prepare("INSERT INTO prediction_points(id,prediction_id,scenario,point_at,price_cents,lower_cents,upper_cents) VALUES(?,?,?,?,?,?,?)").bind(id("prediction_point"),predictionId,point.scenario,point.pointAt,point.priceCents,point.lowerCents??null,point.upperCents??null).run()});return Response.json({id:predictionId,symbol:input.symbol,horizon:input.horizon,confidence:input.confidence},{status:201})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Prediction could not be saved"},{status:400})}}

export async function PATCH(request:Request){try{const{db,householdId}=await workspace(request),body=await request.json()as{predictionId?:string;actualPriceCents?:string;actualAt?:string},actualAt=body.actualAt||new Date().toISOString();if(!body.predictionId||!/^\d+$/.test(String(body.actualPriceCents))||!Number.isFinite(Date.parse(actualAt)))return Response.json({error:"predictionId, actualPriceCents, and a valid actualAt are required"},{status:400});const prediction=await db.prepare("SELECT p.id,p.input_snapshot_json FROM predictions p WHERE p.id=? AND p.household_id=?").bind(body.predictionId,householdId).first<any>();if(!prediction)return Response.json({error:"Prediction not found"},{status:404});const point=await db.prepare("SELECT price_cents::text,lower_cents::text,upper_cents::text FROM prediction_points WHERE prediction_id=? AND scenario='BASE' ORDER BY ABS(EXTRACT(EPOCH FROM(point_at-?::timestamptz))) LIMIT 1").bind(body.predictionId,actualAt).first<any>();if(!point)return Response.json({error:"Base scenario point not found"},{status:409});const snapshot=typeof prediction.input_snapshot_json==="string"?JSON.parse(prediction.input_snapshot_json):prediction.input_snapshot_json,startingPriceCents=String(snapshot?.startingPriceCents||"");if(!/^\d+$/.test(startingPriceCents))return Response.json({error:"Prediction is missing startingPriceCents and cannot be evaluated"},{status:409});const evaluation=evaluatePrediction({basePriceCents:String(point.price_cents),lowerCents:point.lower_cents,upperCents:point.upper_cents,startingPriceCents,actualPriceCents:String(body.actualPriceCents)});await db.prepare("INSERT INTO prediction_evaluations(prediction_id,actual_price_cents,actual_at,direction_correct,inside_expected_range,absolute_error_bps) VALUES(?,?,?,?,?,?) ON CONFLICT(prediction_id) DO UPDATE SET actual_price_cents=excluded.actual_price_cents,actual_at=excluded.actual_at,direction_correct=excluded.direction_correct,inside_expected_range=excluded.inside_expected_range,absolute_error_bps=excluded.absolute_error_bps,evaluated_at=CURRENT_TIMESTAMP").bind(body.predictionId,body.actualPriceCents,actualAt,evaluation.directionCorrect,evaluation.insideExpectedRange,evaluation.absoluteErrorBps).run();return Response.json({predictionId:body.predictionId,evaluation})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Prediction could not be evaluated"},{status:400})}}
