import{workspace,id}from"@/lib/db";import{encryptSecret}from"@/lib/crypto";
export async function POST(r:Request){try{const{db,householdId}=await workspace(r),b=await r.json()as{publicToken?:string;institutionName?:string};if(!b.publicToken)return Response.json({error:"publicToken required"},{status:400});if(!process.env.PLAID_CLIENT_ID||!process.env.PLAID_SECRET)return Response.json({error:"Plaid credentials are not configured"},{status:503});const host=process.env.PLAID_ENV==="production"?"https://production.plaid.com":process.env.PLAID_ENV==="development"?"https://development.plaid.com":"https://sandbox.plaid.com";const response=await fetch(`${host}/item/public_token/exchange`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({client_id:process.env.PLAID_CLIENT_ID,secret:process.env.PLAID_SECRET,public_token:b.publicToken})});if(!response.ok)return new Response(await response.text(),{status:response.status,headers:{"Content-Type":"application/json"}});const data=await response.json()as{access_token:string;item_id:string};const connectionId=id("conn");await db.prepare("INSERT INTO connections(id,household_id,provider,institution_name,status,encrypted_access_token) VALUES(?,?,?,?,?,?)").bind(connectionId,householdId,"plaid",b.institutionName||"Connected institution","active",await encryptSecret(data.access_token)).run();return Response.json({id:connectionId,status:"connected"},{status:201})}catch(e){if(e instanceof Response)return e;throw e}}
