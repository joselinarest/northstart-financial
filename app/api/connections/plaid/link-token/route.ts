import{workspace}from"@/lib/db";export async function POST(r:Request){try{const{userId}=await workspace(r);if(!process.env.PLAID_CLIENT_ID||!process.env.PLAID_SECRET)return Response.json({error:"Plaid credentials are not configured"},{status:503});const host=process.env.PLAID_ENV==="production"?"https://production.plaid.com":process.env.PLAID_ENV==="development"?"https://development.plaid.com":"https://sandbox.plaid.com";const response=await fetch(`${host}/link/token/create`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({client_id:process.env.PLAID_CLIENT_ID,secret:process.env.PLAID_SECRET,client_name:"Northstar",language:"en",country_codes:(process.env.PLAID_COUNTRY_CODES||"US").split(","),products:(process.env.PLAID_PRODUCTS||"transactions").split(","),user:{client_user_id:userId},webhook:process.env.PLAID_WEBHOOK_URL||undefined})});return new Response(await response.text(),{status:response.status,headers:{"Content-Type":"application/json"}})}catch(e){if(e instanceof Response)return e;throw e}}
