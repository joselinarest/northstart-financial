import{workspace}from"@/lib/db";
export const dynamic="force-dynamic";
export async function GET(request:Request){try{const{db,householdId}=await workspace(request),url=new URL(request.url),unread=url.searchParams.get("unread")!=="false",result=await db.prepare(`SELECT id,severity,type,title,explanation,evidence_json,read_at,created_at FROM alerts WHERE household_id=? ${unread?"AND read_at IS NULL":""} ORDER BY created_at DESC LIMIT 100`).bind(householdId).all();return Response.json({alerts:result.results},{headers:{"Cache-Control":"private, no-store"}})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Alerts unavailable"},{status:500})}}
export async function PATCH(request:Request){try{const{db,householdId}=await workspace(request),body=await request.json()as{id?:string;read?:boolean};if(!body.id)return Response.json({error:"Alert id is required"},{status:400});const updated=await db.prepare("UPDATE alerts SET read_at=? WHERE id=? AND household_id=? RETURNING id,read_at").bind(body.read===false?null:new Date().toISOString(),body.id,householdId).first();return updated?Response.json(updated):Response.json({error:"Alert not found"},{status:404})}catch(error){if(error instanceof Response)return error;return Response.json({error:error instanceof Error?error.message:"Alert could not be updated"},{status:400})}}
