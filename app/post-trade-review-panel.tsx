import type {buildPostTradeReview} from '@/lib/post-trade-review';
export default function PostTradeReviewPanel({reviews}:{reviews:{ticker:string;review_json:ReturnType<typeof buildPostTradeReview>}[]}){
 const number=(n:number|null)=>n===null?'Unavailable':n.toLocaleString(undefined,{maximumFractionDigits:2});
 return <section style={{display:'grid',gap:12,minWidth:0,overflowWrap:'anywhere'}}><h3>Post-Trade Reviews</h3>{!reviews.length&&<p>No executed sale reviews yet. Reviews are created by the server lifecycle job, including partial exits.</p>}{reviews.map(({ticker,review_json:r})=><details key={r.exitId}><summary>{ticker} · {r.status} · {r.sellReason||'Unclassified sale'}</summary><p>Sold {r.actual.shares} shares at {number(r.actual.exitPrice)}. Realized result {number(r.actual.realizedResult)} · Return {number(r.actual.realizedReturnBps)} bps.</p><p>{r.entryAssessment}</p><p>{r.stopAssessment}</p><p>MFE {number(r.maximumFavorableExcursionBps)} bps · MAE {number(r.maximumAdverseExcursionBps)} bps</p><p>{r.holdComparison.soldTooEarly}. Excess versus HOLD {number(r.holdComparison.excessVsHold)} · Missed upside {number(r.holdComparison.missedUpside)} · Avoided loss {number(r.holdComparison.avoidedLoss)}.</p><details><summary>Original prediction and evidence</summary><pre style={{whiteSpace:'pre-wrap',overflowWrap:'anywhere'}}>{JSON.stringify(r.predicted,null,2)}</pre></details>{r.evidenceAttribution.map((e,i)=><p key={i}>{e.name}: {e.assessment}. {e.qualification}</p>)}{r.lessons.map((l,i)=><p key={i}>{l}</p>)}<p>{r.limitations.join(' ')}</p><small>Observed through {r.asOf} · Model {r.modelVersion} · Strategy {r.strategyVersion} · No automatic rule changes</small></details>)}</section>;
}
