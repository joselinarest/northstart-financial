export default function FundamentalPanel({profile,metrics}:{profile?:Record<string,any>;metrics?:Record<string,any>}){return <section><h3>{profile?.name||'Business fundamentals'}</h3><p>{profile?.finnhubIndustry||profile?.description||'Company profile unavailable'}</p><dl className="workspace-metrics">{Object.entries(metrics||{}).filter(([,v])=>v!==null&&typeof v!=='object').map(([key,v])=><div key={key}><dt>{key}</dt><dd>{String(v)}</dd></div>)}</dl>{!Object.keys(metrics||{}).length&&<p>Financial metrics unavailable from the provider.</p>}</section>}
