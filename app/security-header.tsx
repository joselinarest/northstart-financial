import RecommendationSummary from './recommendation-summary';
export default function SecurityHeader({symbol,company,price,market,account,strategy,shares,action,onPlan}:{symbol:string;company:string;price:number|null;market:string;account:string;strategy:string;shares:number;action:string;onPlan:()=>void}){return <header className="security-header"><div><h2>{symbol} <small>{company}</small></h2><p>{account} · {strategy} · {shares.toLocaleString()} shares owned</p></div><div><strong>{price&&price>0?`$${price.toFixed(2)}`:'Price unavailable'}</strong><small>{market}</small></div><RecommendationSummary action={action}/><button type="button" onClick={onPlan}>Review trade plan</button></header>}
