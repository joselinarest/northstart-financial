"use client";

type Row=Record<string,any>;
const money=(value:number)=>value.toLocaleString(undefined,{style:"currency",currency:"USD",minimumFractionDigits:0,maximumFractionDigits:0});
export default function DebtLiabilityCenter({accounts,onOpenAccount}:{accounts:Row[];onOpenAccount?:(id:string)=>void}){
 const debts=accounts.filter(account=>/credit|loan|mortgage|line of credit/i.test(`${account.type||""} ${account.subtype||""}`)&&!/investment|brokerage|401|ira/i.test(`${account.type||""} ${account.subtype||""} ${account.name||""}`)),total=debts.reduce((sum,account)=>sum+Math.abs(Number(account.current_balance_cents||0)/100),0),limits=debts.reduce((sum,account)=>sum+Math.max(0,Number(account.credit_limit_cents||0)/100),0),utilization=limits?total/limits*100:null;
 return <section className="debt-liability-center"><header><div><span>DEBT & LIABILITIES · INVESTMENTS EXCLUDED</span><h2>What the household owes</h2><p>This page tracks credit cards, mortgages, and loans. It helps prioritize payments and protect cash before additional investing.</p></div><strong>{money(total)}<small>total connected debt</small></strong></header><div className="debt-explainer"><article><b>Debit is not debt</b><p>A debit-card purchase removes money from checking. It appears under Cash Flow & Spending—not here.</p></article><article><b>A liability is money owed</b><p>Credit-card balances, mortgages, auto loans, student loans, and credit lines belong on this page.</p></article><article><b>What to do here</b><p>Review balances, utilization, interest rates, minimum payments, and which debt should be reduced first.</p></article></div><div className="debt-summary"><span><small>Debt accounts</small><b>{debts.length}</b></span><span><small>Credit utilization</small><b>{utilization===null?"Limit unavailable":`${utilization.toFixed(0)}%`}</b></span><span><small>Priority</small><b>{utilization!==null&&utilization>30?"Reduce revolving balances":"Review rates and payments"}</b></span></div><div className="debt-account-list">{debts.map(account=><button key={account.id} onClick={()=>onOpenAccount?.(String(account.id))}><span><b>{account.name}</b><small>{account.subtype||account.type} {account.mask?`· •••• ${account.mask}`:""}</small></span><strong>{money(Math.abs(Number(account.current_balance_cents||0)/100))}<small>Open account activity →</small></strong></button>)}{!debts.length&&<p>No connected credit-card, mortgage, or loan balance is available. Connect or add a liability to begin.</p>}</div></section>
}
