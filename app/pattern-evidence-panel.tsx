"use client";
import type {PatternEvidence} from '@/lib/pattern-evidence';
export default function PatternEvidencePanel({patterns,selected,onSelect}:{patterns:PatternEvidence[];selected?:string;onSelect:(id:string)=>void}){
  return <section style={{padding:12,display:'grid',gap:10,minWidth:0,overflowWrap:'anywhere'}}><h3>Validate This Trade · Pattern Evidence</h3><p>Geometry evidence only. A pattern never authorizes an order. Quality is a heuristic score, not a win probability.</p>{!patterns.length&&<p>No qualifying pattern detected in completed available candles. Missing history is not a bearish signal.</p>}{patterns.map(p=><article key={p.id} style={{border:'1px solid #64748b',borderRadius:8,padding:10}}><button type="button" aria-pressed={selected===p.id} onClick={()=>onSelect(p.id)}>{p.name} · {p.timeframe} · {p.validation} · Show on chart</button><p>Quality {p.quality}/100 · Confirm {p.confirmationLevel.toFixed(2)} · Invalidate {p.invalidationLevel.toFixed(2)}</p><p>Volume {p.volumeConfirmation} ({p.relativeVolume}×) · Trend {p.trendAlignment} · Market {p.marketAlignment} · Sector {p.sectorAlignment} · False-breakout risk {p.falseBreakoutRisk}</p><p>{p.missingConfirmation.length?p.missingConfirmation.join('; '):'Evidence checks passed. Central decision, entry confirmation and user-approved execution are still required.'}</p></article>)}</section>;
}
