# Northstar blocker audit — 2026-09-23

Audit scope: scanned 232 application/library source files, inventoried 85 API routes and 47 environment-variable references, executed all 24 scripts matching scripts/*-check.mjs, full ESLint, TypeScript and the production build. Attempted one mobile browser regression and read AWS deployment, scheduler, metrics and worker logs. This does not mean every API was exercised against production.

## Actual run results

- Script checks: 16 PASS, 7 FAIL, 1 BLOCKED.
- TypeScript: PASS. Production build: PASS; Next configuration skips lint.
- Full ESLint: FAIL — 343 errors and 67 warnings, across 81 files containing errors (267 files evaluated).
- Browser regression: BLOCKED before browser launch — missing Playwright chromium_headless_shell-1243 executable. A direct Next production server starts successfully, overcoming the initial Playwright webServer startup failure. Authenticated E2E storage is also absent.
- No application fixes, migrations, deployment, provider connections or trades were performed in this audit.

## Confirmed external/runtime blockers

| ID | Area | Actual evidence / why blocked | Required next step |
|---|---|---|---|
| B01 | Production background monitoring, notifications and scheduled analysis | Enabled one-minute EventBridge rule exists, but Lambda recorded 58 Errors in the queried last-hour window. Sample worker log messages are exactly “Northstar worker returned 500”. This prevents certifying always-on operation. The underlying application exception is not yet identified; an ERROR-filtered recent Amplify log query returned no matching events. | Trace server-side /api/notifications/process failures and verify successful processing and deliveries afterward. |
| B02 | Current code in production | Amplify master latest job 168 SUCCEED deployed commit 9ee30e959e38083c19d81009d44a1902cd1c4e42 on September 18. Local HEAD is 9ac9dee and newer modifications are uncommitted. | Resolve release failures, commit/push the intended revision and verify a successful deployment of that exact revision. Git authentication was not retested in this audit. |
| B03 | Fidelity authorization/OAuth | Plaid Dashboard sign-in remains unresolved in the supplied browser state. Fidelity enablement and Data Transparency have not been verified. No real Production Fidelity OAuth and holdings result exists in this audit. | Complete authorized Dashboard sign-in, verify client-specific Fidelity/OAuth/use-case access, then perform the ordered production connection test. Access is UNVERIFIED, not proven disabled. |
| B04 | Live database tests and persisted-account reload checks | DATABASE_URL and NORTHSTAR_SECRET_ID are absent from this local process; only .env.example exists locally. notification-clear-check fails with DATABASE_URL is required. | Supply an authorized test environment or configured server secret reference; run migrations and authenticated persistence regressions there. This does not establish that production secrets are missing. |
| B05 | Live quotes, fundamentals, news, AI and Plaid tests | Local process has no Plaid, Alpaca, Finnhub or OpenAI credentials and no runtime secret reference. | Run with authorized provider configuration; verify freshness, endpoint errors and entitlement behavior rather than mock responses. |
| B06 | Real push/email delivery and device acceptance | Local VAPID, email and CRON_SECRET configuration is absent. Existing tests inject delivery outcomes. Production worker is failing (B01). | Verify configured production channels, granted device subscriptions and delivery/retry/deep-link behavior with app closed. |
| B07 | Mobile charts, fullscreen and manual-account E2E | Playwright browser executable and NORTHSTAR_E2E_STORAGE_STATE are missing. 360px Today test cannot launch Chromium. | Install the matching test browser and provide authenticated test storage; run all required viewports and interactions. |
| B08 | AI production readiness | Engine requires an approved strategy and CHAMPION model. Migration seeds lifecycle-1.0.0 as DRAFT. Production registry contents were not queried. | Inspect registry and controlled evaluation evidence; approve a validated version explicitly. Do not silently bypass approval. |

## Code/test gaps blocking acceptance (not external permission blockers)

| ID | Area | Evidence / unfinished work |
|---|---|---|
| C01 | Regression suite | Seven source-assertion checks fail (listed below). Some assertions target old source layout/formatting; failure is not proof of seven runtime bugs. They need behavioral verification and legitimate test updates. |
| C02 | Full lint | 343 errors / 67 warnings. Build passes only because lint is skipped. Common issues include explicit any, prefer-const and JSX escaping. |
| C03 | Separate DAY_TRADE / SWING positions and risk | strategy_positions/fills tables exist, but application usage is not wired. strategyRiskBudget has no caller. Separate executions, stop ownership, P/L, combined-loss blocking and separate Today plans are not complete. |
| C04 | Settings propagation / wizard | Global settings are saved/read in Configuration Center but not consumed across timestamp displays and scheduled workers. Risk defaults are created when settings are read, not guaranteed at account creation. Wizard is a checklist. Full timezone/schedule/risk propagation is incomplete. |
| C05 | Provider health accuracy | Market Data test checks the clock, yet its HEALTHY state can enable day trading; this does not prove usable intraday candles. Other provider checks often accept HTTP 200 without validating usable data. Push/email end-to-end health is not proven. |
| C06 | Shared configuration | 47 env keys are referenced throughout app/lib; APP_URL is duplicated in .env.example. A fully consolidated validated configuration layer and comprehensive browser-secret audit are incomplete. |
| C07 | Central recommendation consistency | currentAccountDecisions reader exists without callers; legacy continuous loop still submits a reduced independent snapshot before lifecycle evaluation. All-page consistency acceptance remains incomplete. |
| C08 | Options entries | Option candidates do not supply entryPlan, while central candidate safety requires one for entries. Options buys remain gated. Separate underlying/premium trigger and order confirmation implementation is needed. |
| C09 | Complete post-trade review attribution | Reviews exist, but original entry confirmation/stop linkage and multi-lot attribution are incomplete. Options premium paths are not available. Unknown metrics are deliberately left unknown, not assigned successful values. |
| C10 | Pattern accuracy and chart UX | Synthetic tests cover 26 labels; real-market false-positive rates and browser/touch overlays are not validated. Client pattern context correctly remains UNKNOWN for independent data it lacks. |
| C11 | Full live regression / endpoint coverage | 85 routes were inventoried, not all executed with real authenticated state. Manual holdings reload/edit/delete, session-aware Today, holiday behavior and mobile chart acceptance still require authenticated provider-backed tests. |

## Check scripts

| Script | Result |
|---|---|
| account-candidate-ranking-check.mjs | FAIL |
| account-goal-profile-check.mjs | PASS |
| ai-decision-engine-check.mjs | FAIL |
| catalyst-entry-gate-check.mjs | FAIL |
| chart-confluence-check.mjs | PASS |
| continuous-intelligence-loop-check.mjs | FAIL |
| critical-workflows-check.mjs | PASS |
| daily-close-review-check.mjs | PASS |
| entry-plan-check.mjs | PASS |
| foundation-audit-check.mjs | PASS |
| kids-planning-check.mjs | PASS |
| market-alert-check.mjs | FAIL |
| market-discovery-check.mjs | PASS |
| mobile-ux-check.mjs | PASS |
| notification-clear-check.mjs | BLOCKED |
| notification-events-check.mjs | PASS |
| pattern-review-check.mjs | PASS |
| plaid-investments-check.mjs | PASS |
| pwa-check.mjs | PASS |
| system-capability-check.mjs | FAIL |
| tactical-rebuy-check.mjs | PASS |
| trade-lifecycle-check.mjs | PASS |
| trade-lifecycle-integration-check.mjs | PASS |
| transaction-alert-check.mjs | FAIL |

Failing assertions: account-candidate-ranking expects TOP_OPPORTUNITY; ai-decision-engine expects calibrationFactor in the old module; catalyst-entry-gate expects the old source expression; continuous-intelligence-loop expects Today health proof and Settings diagnostic; market-alert expects the old NOTIFICATION_DEEP_LINK handler; system-capability expects exact JSX formatting; transaction-alert expects the old COALESCE(read_at,CURRENT_TIMESTAMP) SQL.

Machine-readable detail: audit-results/checks.json, audit-results/eslint.json, audit-results/environment-inventory.json. Per-check logs are local under audit-results. No secret values were printed or included in this report.
