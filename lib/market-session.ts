import type{MarketSession}from"@/lib/providers/market-data";
export function marketSessionAt(value:Date|string|number):MarketSession{const date=new Date(value),parts=Object.fromEntries(new Intl.DateTimeFormat("en-US",{timeZone:"America/New_York",weekday:"short",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts(date).filter(part=>part.type!=="literal").map(part=>[part.type,part.value])),weekday=!['Sat','Sun'].includes(String(parts.weekday)),minutes=Number(parts.hour)*60+Number(parts.minute);if(!weekday)return"CLOSED";if(minutes>=240&&minutes<570)return"PREMARKET";if(minutes>=570&&minutes<960)return"REGULAR";if(minutes>=960&&minutes<1200)return"AFTER_HOURS";return"CLOSED"}
export function formatMarketTime(value:string,timeZone:string){try{return new Intl.DateTimeFormat("en-US",{timeZone,weekday:"short",month:"short",day:"numeric",hour:"numeric",minute:"2-digit",timeZoneName:"short"}).format(new Date(value))}catch{return new Intl.DateTimeFormat("en-US",{timeZone:"America/Phoenix",weekday:"short",month:"short",day:"numeric",hour:"numeric",minute:"2-digit",timeZoneName:"short"}).format(new Date(value))}}

